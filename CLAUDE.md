# 微光小鎮·3D 實驗線 — 給 Claude 的常駐規則

業主 2026-09-24 開線。這份檔案對本倉庫的每一個 Claude session 都有效；設計的來龍去脈看 `docs/D000-vision.md`。

## 這是哪條線

- **第三條線：3D 實驗線。**
  - 倉庫：`lijiabao1998/GlimmerTown3D-lab`（公開）
  - 本地：`C:\dev\GlimmerTown3D-lab`。刻意不放在 OneDrive：主線曾被 OneDrive 毫秒級回退 `index.html`。
- **另外兩條線都是 2D，本線只讀**，業主明說才能動：
  - 主線 `lijiabao1998/GlimmerTown`（本地 `C:\dev\glimmer-town`）：嚴格紀律，四方治理，上萬條斷言。
  - 2D 實驗線 `lijiabao1998/GlimmerTown-lab`（本地 `OneDrive\Desktop\新的迭代`）：美術第一。
- **只參考 2D 實驗線**（業主 2026-09-25）。規則、數字、美術語言都以實驗線為準；主線不作參考，包括它的 Python 工具鏈。搬過來照樣寫明出處。
- **定位是 Pre**（業主 2026-09-25）。本倉庫是前期實驗；內容做到一大段之後，業主可能從這裡 fork 出主線。
- **卡號一律用 D 開頭**（D001、D002……），不跟兩條 2D 線的 T 系列對齊，也不借號。兩條 2D 線已經在 T578–T589 撞過號，不能再多一條。
- **串台要及時提醒。** 從 2D 線搬程式、規則或數字過來時，寫明出處（哪條線、哪個 commit）。2D 線的規矩不會自動適用這裡，拿不準就先問業主。

## 核心概念（一句話）

**同一座城、三種時間、三種玩法**：建造（3D 版 GlimmerTown）、一生（陪一個市民活完一輩子）、考古（數百年後走進廢墟，讀出這座城的歷史）。三種玩法共用同一個地基：**世界會記住自己的歷史**。

**目前全力做建造**（業主 2026-09-25）：一生、考古暫停，世界歷史照樣記。

## 常駐規則

1. **全局觀感先給業主看圖。** 畫風、光影、色調、密度這類一改就影響整張畫面的東西，先做兩三檔、拍同一個畫面的對照圖讓業主挑，不替業主選。
   - **業主 2026-09-25 補充**：「直接做吧，就跟 lab 線一樣，開始是美術好看，後來長出了自己的標準。」美術上 Claude 可以自己判斷、直接做。
   - 對照圖照拍。選了哪一檔、為什麼選，寫在卡面；業主看了不喜歡就改回來。
   - 定畫風（D001 已定 A）這種整條線的大事，仍先給業主挑。
2. **模擬和渲染分開。** 模擬不 import 任何渲染程式，渲染不改世界狀態。美術怎麼重畫，都不能順便炸掉模擬。
3. **模擬必須決定性。** 用固定種子的亂數，同種子得同結果。模擬裡不准用 `Math.random()` 或現實時間。
4. **世界歷史只增不改。** 蓋、拆、改建、入住、遷出都記成事件；三種玩法都從同一份歷史讀。歷史格式一改，存檔就要加版本號、舊檔要能讀。
5. **手機優先。** 效能預算以中階 Android 手機為準；桌機跑得動不算數。
6. **每一輪的流程**：出卡（`docs/Dxxx-標題.md`，驗收條件寫在動手之前）→ 施工 → 守衛（可斷言的事實，不是「看起來對」）→ 煙霧測試綠燈 → commit → push → `LOG.md` 記一列。**「沒做成的事」不准空著，也不准美化。**
7. **原始碼零外部素材**（業主 2026-09-25）。圖片、字型、模型、音效都不從外部拿，畫面全部由程式生成。2D 實驗線的美術規矩（光從左邊來、先有燈具才有光、變體要結構不同）可以參考，但不會自動變成本線的規則。
8. **規則要跟實驗線對拍**（業主 2026-09-25）。
   - **怎麼比**：從實驗線搬規則時，同一個起點分別跑實驗線（無頭，用 `GV.step` 逐日推進）和本線，逐項比對。
   - **存證**：實驗線那邊的數字連同實驗線 commit 存成黃金樣本。
   - **標準**：公式層要逐項相等；整座城的軌跡先求多種子統計一致，能逐位元組相等的地方就要相等。
   - **不能跨讀檔**：實驗線讀檔會用 `seed^day` 重設亂數，所以對拍的比較區間裡不能有讀檔。
   - **工具**：用 Node，不另裝 Python。D003 的分享碼解碼已經這樣對過。

## 版本一致：本機 ⇄ 雲端（單一寫入者；跟兩條 2D 線同一套）

本機和雲端看不到對方，唯一的交會點是 GitHub 上的 `origin/main`。所以「版本一致」的定義是：**開工時自己的 HEAD＝`origin/main`，收工時 `origin/main`＝自己的 HEAD。**

- **開工**（本機或雲端都一樣，寫任何檔案之前）：

  ```bash
  git fetch origin
  git status -sb
  git rev-parse HEAD origin/main      # 印出兩行，兩行相同才算一致
  ```

  - 兩個 hash 相同、工作樹乾淨：開工。
  - behind：`git pull --ff-only`，再核一次。
  - 分岔（同時 ahead 又 behind）：停手、寫日誌、等業主。不准 merge 別人的歷史，更不准 `--force`。
  - 有不是自己留下的未提交檔案：先原樣凍結成一個 commit 再審，不要順手刪，也不要混進自己的提交。
- **收工**：綠燈 commit 之後 `git push`，再 `git fetch` 核一次，HEAD＝`origin/main` 才算收工。
- **換邊施工**（本機換雲端，或雲端換本機）：離開的那一方要先收工到「HEAD＝`origin/main`、工作樹乾淨」。雲端看不到本機的硬碟，沒推上去的東西，對雲端來說就不存在。雲端開工前，如果業主說本機還有東西沒推，先等本機推完。
- **同一時間只准一方施工。** 開工時 fetch 到新提交，就代表剛有人在寫。

**雲端 session 對照**：本地路徑 `C:\dev\GlimmerTown3D-lab` 在雲端不存在，等於「本倉庫根目錄」。推送一律推到 `origin main`，只准快進；session 另外指定了分支，就一併推。
