# D010 起步城逐日模擬

**基線**：`d1f56d8`
**參考**：2D 實驗線 `lijiabao1998/GlimmerTown-lab` @ `d23c18d`（v13.43／T638），只讀。行號都指 `index.html` @ `d23c18d`。
**授權**：
- 業主 2026-09-25「剩下的你繼續做吧」。
- 拆卡見 D009 卡；規則搬移照 CLAUDE.md 規則 8。

## 為什麼做這張

D009 把生長這一層的公式逐條搬過來、逐項對拍，但它們還沒接起來，城市不會動。這張把它們接成每天推進的模擬：
- 一座只有路、分區、一座電廠、一間警局的起步城，看它自己長起來；
- 同時跟實驗線從同一個起點跑出來的城市比一比，差多少。

玩家自己蓋東西是 D011。

## 做什麼

1. **起步城**：一張實驗線分享碼，兩邊都讀這一張。
   - **地形**：取種子城的地形（地形、高地、樹），其他全部清空。
   - **佈局**：在陸地上放一小片起步佈局（棋盤路網、住商工三區、一座燃煤電廠 k5、一間警察局 k11），由 `src/content/starter.ts` 決定性產生。
   - **對帳**：實驗線 `GV.importCode` 讀回的對帳數字要跟本線解碼一樣（同 D003、D007 的做法）。
2. **逐日推進** `src/sim/day.ts`：只接線，公式一律用 D009 的 `src/sim/rules/`。
   - **順序**：照實驗線 `tick()` 的順序，每一步註明行號：
     1. 日數 +1；
     2. 天氣；
     3. 地價靜態基準（有人蓋或拆東西才重算，實驗線 `landDirty`）；
     4. 當日電力容量與道路帶電；
     5. 逐棟：通電、住宅幸福、人口、商工職位；
     6. 名目就業、勞動市場；
     7. 三條需求；
     8. 移民潮、人口學係數；
     9. 生長；
     10. 升級。
   - **補兩個場**（照實驗線原式，也要對拍）：
     - **服務覆蓋**：`stampCov`，方框半徑 `COVR`；起步城用到警局、電廠，公園、學校也一起。
     - **污染**：`stampPolSrc`、`stampPolTree`，工業、電廠是污染源，樹減免。
   - **上層系統一律接實驗線自己「還沒就緒」的回退值，名單記在卡上**：經濟閉環、住房市場、企業、T471 分時調度、通勤、噪音、事故、災害、火災、犯罪、疾病、水、污水、財政。
3. **世界歷史**（規則 4）：每一棟長出來、每一次升級都記成事件。
   - 格式：`{ day, t: 'grow' | 'upgrade', x, z, k, lv, v }`，只增不改。
   - `CITY_FORMAT` 1 → 2；舊格式（只有匯入那一筆）照讀。
4. **3D 畫面**：
   - 城市選單多一個「起步城」；
   - 播放、暫停，速度三檔；
   - 顯示第幾天、人口、住商工各幾棟；
   - 每隔幾天重建一次場景，用 B 檔的街區切分。
5. **跟實驗線對照**：
   - **兩邊怎麼跑**：
     - 實驗線無頭匯入起步城碼，開舊版供電（`__legacyPower450`，對應 D009 F11），用 `GV.step` 逐日推進 120 天；
     - 本線同樣跑 120 天。
   - **種子**：換存檔的 `seed` 欄位，跑 8 個。實驗線讀檔會用 `seed^day` 重設亂數，所以比較區間裡不讀檔（規則 8）。
   - **每天記**：人口、就業、住商工各幾棟、各級幾棟、三條需求、城市幸福。

## 不改什麼

- **D009 的公式**：不改一個字，只接線。改公式要回 D009 的守衛。
- **既有的畫面與資料**：兩座樣本城、全種類樣張城、300 年示範都不動。
- **不做玩家建造**（D011）：鋪路、劃區、放電廠、拆除都不在這張。
- **實驗線只讀**。

## 驗收（動手前寫）

1. **決定性**：同一個種子跑 120 天，最終狀態的雜湊兩次相同；不同種子不同。模擬層純度守衛照舊。
2. **只接線**：`day.ts` 的每一步都呼叫 `src/sim/rules/` 的函式，不自己重寫公式；每一步註明實驗線行號。
3. **新補的兩個場對拍**：服務覆蓋、污染照 D009 的方法，從實驗線原始碼求值做黃金樣本，≥200 張隨機小圖逐格相等，注入單點突變要紅。
4. **起步城碼對帳**：實驗線讀回的對帳數字＝本線解碼。
5. **歷史重播**：120 天後，從匯入事件加上逐筆生長、升級事件重播，得到的建築清單跟模擬結束時逐欄相同；只有匯入事件的舊格式照讀。
6. **跟實驗線對照（量出來、不設通過門檻）**：
   - **存證**：8 個種子 × 120 天的逐日數字，兩邊都存成樣本（實驗線那份記 commit）。
   - **卡面列表**：列出第 30、60、120 天兩邊的均值 ± 標準差與差距。
   - **差距原因**：差距最大的三項寫出推測原因，指到哪一個沒搬的系統。
   - **為什麼不設門檻**：上層系統還沒搬，軌跡不會一致，所以這一項只量不判；「統計一致」留給之後逐層加系統的卡。
7. **畫面**：
   - 起步城在 3D 裡看得出長起來。
   - 對照圖：第 0、30、60、120 天，每一天都配實驗線同一天的 2D。
   - 手機直式一張。
8. **手機預算**：
   - 起步城第 120 天：三角形 ≤ D003 種子城基線的 1.5 倍（118,884），draw call ≤ 18；
   - 重建一次場景，在 CPU 降速 6 倍下 ≤ 400 ms；
   - 推進一天（不含重建），在桌機上 ≤ 5 ms。
9. **上線**：Node 守衛、煙霧、Actions、Pages 綠。

## 施工紀錄

雲端施工，2026-09-26。開工時 `origin/main`＝`0807797`（本卡），HEAD 一致、工作樹乾淨。施工中兩個存檔提交只推在 session 分支（`debac4e`、`f278960`），收工提交一起快進到 `main`。

### 做了什麼

1. **起步城**（`src/content/starter.ts`、`src/content/samples/starter.code.txt`、`starter.json`）
   - 4×4 個街廓、每個 4×4 格、路寬 1 格，共 21×21 格；道路 2 級（實驗線「道路」工具的等級，51161）。
   - 分區：住宅為主，商業兩塊在第 2 排中間，工業兩塊在東南角；燃煤電廠 k5 在東南角最外那一格，警察局 k11 在城心路口西北那一格。
   - 位置：整塊 21×21 都是平坦草地的候選裡，中心離地圖中心最近的那一個（種子城是 (26,22)）。地形、高地、樹沿用種子城，用到的格子清掉樹。
   - 分享碼由 `node tools/lab-extract.mjs --part=d010` 從種子城存檔改出來；實驗線 `GV.importCode` 讀回：路 185、分區 192／31／31、建築 2，逐格道路等級 `{"2":185}`，都跟本線解碼相同。
2. **逐日推進**（`src/sim/day.ts`）
   - `simFromSave` 照實驗線 `load()`（66863 起）建模擬狀態：`mulberry32(seed^day)`、天氣 `wxT=3+ri(5)`、新城的人口／幸福／需求初值。
   - `stepDay` 照 `tick()` 順序接 D009 的公式，25 個行號依序註明、31 個規則函式都真的呼叫。
   - 新補的場在 `src/sim/rules/fields.ts`：`stampCov`、`stampPolSrc`、`stampPolTree`、`recomputePol`、`rebuildLandBase`、`rebuildCov`、`fieldsOf`，以及 `COVR`、預算類別、污染源常數表。
   - **卡面第 3 步更正**：實驗線的地價基準不是「有人蓋或拆才重算」。`--diag` 實跑兩種設定各 120 天，每天收尾 `landDirty` 都是真、`landBox` 都是空，也就是**每天整張重算**。本線只重算污染變了的那一框，另跑一次整張重算版，120 天雜湊逐位相同（守衛）。
3. **世界歷史**：`CITY_FORMAT` 1 → 2。
   - 生長、升級照卡面格式記成事件，只增不改。
   - `src/sim/replay.ts` 從匯入事件加逐筆事件重播出建築清單；格式 1（只有匯入那一筆）照讀。
4. **3D 畫面**（`src/cityView.ts`）
   - 城市選單多「起步城」；播放、暫停；速度 1／3／10 天每秒。
   - 顯示第幾天、人口、就業、住商工棟數與二級棟數、幸福、三條需求。
   - 有長或升級的話，每 5 天用 B 檔街區切分重建一次場景。
   - 建築卡會列出「第幾天長出來（逐日模擬）」「第幾天升到幾級」。
5. **跟實驗線對照**（`tools/lab-compare.mjs`）
   - 8 個種子 × 120 天，兩種實驗線設定都跑，存 `src/content/samples/d010-lab.json`（記實驗線 commit `d23c18d`）與 `d010-3d.json`。
   - **卡面第 5 項更正**：只開 `__legacyPower450` 不夠，還要加 `__legacyPower471`。只開 450 時，每天收尾的財政等呼叫端仍走 T471 分時調度，把所有商工設成沒電，就業整段是 0。這是審查抓到的，下面有記。
   - 另加一個**回退設定**：實驗線有開關的上層系統都關成「沒就緒／舊式」，災害關掉。它跟本線接的回退值一致，是主要的比較對象。

### 上層系統：三類

卡面原本寫「上層系統一律接實驗線自己的回退值」。對讀之後分成三類，名單以這張表為準：

| 類 | 意思 | 系統（實驗線開關或行號） |
|---|---|---|
| 1 | 實驗線有開關，本線接它關掉時的值 | 住房 T488（`__noHousing488`）、企業 T489（`__noEnterprise489`，39560 `enterpriseRollback489`）、T471 分時調度（`__legacyPower450`＋`__legacyPower471`）、舊式供水（`__legacyWater449`）、移動 T491／T509、財政回饋 T510／T515、事故 T493、景氣 T490、公共服務 T495、災害（`glimmerville.v1.ds`＝0），以及 T503–T508、T512 等回退設定裡列的開關 |
| 2 | 實驗線**沒有開關、照跑**，本線沒搬（差距的來源） | 經濟快照 T481／T482（第 1 天收尾就就緒）、通勤 T141、道路負載與壅堵 T129、垃圾、糧食、夜間城市 T487、城市活動 T299、火災、犯罪、廢棄、疾病、死亡與喪事 |
| 3 | 起步城用不到 | 噪音、污水廠、天際線合併等 |

卡面把「通勤」列在回退值名單裡，是錯的：實驗線沒有通勤的開關，改列第 2 類。

### 跟實驗線對照（8 個種子，均值 ± 標準差）

- **本線**：D009 公式照 `tick()` 順序接線，上層系統接回退值。
- **回退**：實驗線關掉有開關的上層系統、關災害，第 2 類照跑。
- **預設**：實驗線預設加舊版供電（`__legacyPower450`＋`__legacyPower471`），災害開。
- **差**：本線減實驗線。
- 第 0 列是讀檔後的快照。實驗線讀檔的 T510 包裝（68519）會先算一次 pop／jobs，兩邊不可比，所以只比第 30、60、120 天。
- 就業、勞動力照實驗線 `truthSnapshot496` 在企業沒就緒時的算法：勞動力＝round(人口×0.6)，就業人數＝min(勞動力, 職位)。
- 完整逐日數字（21 欄，含各級棟數）在兩份 JSON 裡。

| 天 | 指標 | 本線 | 回退 | 差 | 預設 | 差 |
|---|---|---|---|---|---|---|
| 30 | 人口 | 351.8 ± 4.9 | 315.9 ± 29.8 | 35.9 | 250.8 ± 22.7 | 101.0 |
| 30 | 職位 | 154.9 ± 11.5 | 133.6 ± 14.8 | 21.3 | 94.8 ± 18.7 | 60.1 |
| 30 | 幸福 | 0.541 ± 0.011 | 0.252 ± 0.020 | 0.289 | 0.113 ± 0.021 | 0.428 |
| 30 | 需求 住 | −0.407 ± 0.115 | −0.503 ± 0.039 | 0.096 | −0.520 ± 0.046 | 0.114 |
| 30 | 需求 商 | 1.000 ± 0.000 | −0.700 ± 0.014 | 1.700 | −0.792 ± 0.029 | 1.792 |
| 30 | 需求 工 | 0.991 ± 0.025 | −0.235 ± 0.000 | 1.226 | −0.235 ± 0.000 | 1.226 |
| 30 | 住 | 56.8 ± 1.7 | 47.3 ± 4.0 | 9.5 | 38.8 ± 3.6 | 18.0 |
| 30 | 商 | 20.4 ± 1.1 | 0.1 ± 0.4 | 20.3 | 0.5 ± 0.5 | 19.9 |
| 30 | 工 | 11.4 ± 1.6 | 19.4 ± 2.4 | −8.0 | 19.8 ± 1.4 | −8.4 |
| 60 | 人口 | 431.0 ± 19.4 | 363.1 ± 4.5 | 67.9 | 310.6 ± 10.8 | 120.4 |
| 60 | 職位 | 191.4 ± 19.1 | 148.5 ± 6.0 | 42.9 | 41.8 ± 9.5 | 149.6 |
| 60 | 幸福 | 0.418 ± 0.013 | 0.230 ± 0.009 | 0.187 | 0.075 ± 0.018 | 0.343 |
| 60 | 需求 住 | −0.446 ± 0.131 | −0.573 ± 0.057 | 0.127 | −0.432 ± 0.003 | −0.014 |
| 60 | 需求 商 | 1.000 ± 0.000 | −0.721 ± 0.009 | 1.721 | −0.780 ± 0.000 | 1.780 |
| 60 | 需求 工 | 1.000 ± 0.000 | −0.235 ± 0.000 | 1.235 | −0.210 ± 0.027 | 1.210 |
| 60 | 住 | 116.3 ± 4.1 | 54.6 ± 1.1 | 61.6 | 71.9 ± 3.1 | 44.4 |
| 60 | 商 | 29.5 ± 0.9 | 0.1 ± 0.4 | 29.4 | 0.5 ± 0.5 | 29.0 |
| 60 | 工 | 25.0 ± 1.1 | 27.1 ± 2.9 | −2.1 | 24.3 ± 3.8 | 0.8 |
| 120 | 人口 | 444.9 ± 28.5 | 359.6 ± 22.7 | 85.3 | 301.5 ± 12.1 | 143.4 |
| 120 | 職位 | 133.8 ± 29.1 | 99.5 ± 20.5 | 34.3 | 1.5 ± 2.1 | 132.3 |
| 120 | 幸福 | 0.400 ± 0.010 | 0.236 ± 0.026 | 0.164 | 0.050 ± 0.000 | 0.350 |
| 120 | 需求 住 | −0.792 ± 0.108 | −0.821 ± 0.090 | 0.029 | −0.378 ± 0.000 | −0.414 |
| 120 | 需求 商 | 1.000 ± 0.000 | −0.777 ± 0.005 | 1.777 | −0.780 ± 0.000 | 1.780 |
| 120 | 需求 工 | 1.000 ± 0.000 | −0.235 ± 0.000 | 1.235 | −0.050 ± 0.007 | 1.050 |
| 120 | 住 | 134.4 ± 6.3 | 54.0 ± 1.5 | 80.4 | 149.1 ± 6.4 | −14.8 |
| 120 | 商 | 31.0 ± 0.0 | 0.1 ± 0.4 | 30.9 | 0.4 ± 0.5 | 30.6 |
| 120 | 工 | 31.0 ± 0.0 | 22.1 ± 7.3 | 8.9 | 22.8 ± 2.3 | 8.3 |

- **升級**：本線第 120 天有二級住宅 5.1 棟、二級商業 21.5 棟；實驗線兩種設定都沒有二級。
- **分岔日**：回退設定第 1 天那一列，8 個種子 21 欄都跟本線逐項相等（守衛）；8 個種子都從第 2 天起分岔。

### 差距最大的三項：原因

以回退設定為準，下面都有實跑佐證。歸因用 `node tools/lab-compare.mjs --diag --config=<設定>`：記憶體副本插只讀出口，原檔不動；第一個種子，記第 1、2、10、30、60、120 天的幸福組成、需求組成、經濟快照。

1. **商業幾乎不長**：實驗線 0.1 棟，本線 20.4／29.5／31.0 棟。
   - 實驗線的經濟快照 T481／T482 沒有開關，第 1 天收尾就就緒（`ready: true`）。起步城沒有二級商業，零售利用率是 0，`economyDemands481` 把商業需求壓到 −0.70～−0.78。
   - 生長那一段（55609）寫明「需求 < −0.5 該類停長」，p=0，商業整段不長。
   - 本線經濟沒搬（第 2 類），`economyDemands481(…, null)` 走舊式需求，商業需求 1.0。
2. **幸福**：差 0.289／0.187／0.164。實驗線照跑、本線沒搬的扣分，以第 30 天、種子 5162026 為例：
   - 組成之後才扣的垃圾／糧食：−0.139（10～120 天在 −0.08～−0.22 之間）；
   - 通勤 T141：−0.060；
   - 喪事未安撫、死亡：−0.045；
   - 壅堵 T129：−0.016；
   - 夜間城市：−0.002；
   - 合計約 −0.26，佔這一天差距 0.29 的九成。
3. **住宅與人口**：住宅差 9.5／61.6／80.4 棟，人口差 36／68／85。
   - 實驗線住宅需求比本線低：幸福低 → 舊式需求的幸福項 `(cityHappy−.6)×1.2`，第 30 天實驗線 −0.434、本線約 −0.07；再疊經濟快照的混合。
   - 第 30 天實驗線住宅需求已經貼著 −0.5，之後跌破，55609 讓住宅停長，第 40 天起卡在 54～56 棟。
   - 另外 55616（T72）：城市幸福 < 0.4 時住宅生長機率再減半；實驗線整段在 0.23～0.25。
   - 本線住宅需求要到後段才跌破 −0.5，所以一路長到 134 棟。

**預設設定為什麼第 120 天職位幾乎是 0**（跟回退比，不是設定壞掉）：

- **限電**：舊式供電每棟判 `b.pw＝near&&powered<cap`（55155），一座燃煤電廠約供 75～78 棟住商工，按格索引由北往南分配。
- **住宅搶光容量**：預設設定住宅需求有住房市場 T488 撐著，住宅一直長（種子 5162026 第 120 天 161 棟），北邊的住宅先把容量用完。
- **工業全斷電**：南排的工業第 50 天起大批斷電，第 70 天起 27 棟全沒電（企業快照：瓶頸 power 27／27）；實驗線又沒有商業，職位就歸 0。
- **本線同一條規則**：同一個種子第 61 天工業也全沒電，但北邊第 2 排的商業通著電，所以職位還有。
- **幸福另一大塊**：預設設定每天都有「水壓／水質 −0.2」。起步城沒有供水設施，新式供水 T449／T472 判住宅缺水；回退設定用舊式供水，沒有這一項。第 120 天另有「無電 −0.17」。

### 審查

施工後跑了一輪多視角審查。提出 16 條，對抗驗證後成立 10 條，都修了：

1. **「只接線」守衛只看字、不看行為**（major）。三個真的改變模擬的接線突變全綠。修法：
   - 去掉註解之後，每個規則函式都要真的被呼叫；
   - 加**回歸錨點**：8 種子 × 120 天逐日數字＝`d010-3d.json`；
   - 加**實驗線錨點**：回退設定第 1 天＝本線第 1 天，8 種子 × 21 欄。
   - 重跑那三個突變：「關掉每 30 天財富判定」「判定值換成現值」轉紅；「只算有電的住宅」在 8 個種子上雜湊完全不變，是**等價突變**。實驗線 55282–55284 會用全部 k1 住宅重算城市幸福，蓋掉 55254；起步城沒有社宅 k127，差別只在「沒有 k1、只有 k127」時才出現。
2. 勞動力、就業兩欄在兩邊定義不同（人口 0 時本線 1、實驗線 0）→ 兩邊都照 `truthSnapshot496` 算。
3. 模擬推進後，匯入建築的卡片匯入日、存檔屋齡跟著變（major）→ 匯入日改讀匯入事件，屋齡用匯入當天的；煙霧加守衛。
4. 播放中卡片開著，重建後卡片、框還是舊的 → 重建時重寫卡片；煙霧加守衛（1 級 → 2 級）。
5. 播放中切換城市，會先替要丟掉的場景重建一次，計時被灌水 → 載入時先清髒旗、刪舊計時；煙霧加守衛。
6. 播放中呼叫 `__gt.simStep`／`simRebuild` 會多推幾天 → 每一幀拆成推進與畫面兩段，測試只呼叫畫面那段。
7. 「預設＋舊版供電」只開 450，就業整段 0（major）→ 加 `__legacyPower471`，另加警報：整組種子第 10 天起就業都是 0 就擋下。補了之後後段職位仍歸 0，查明是真限電（見上一節）。
8. 實驗線回退設定照跑通勤 T141、壅堵 T129，工具和 `day.ts` 都沒列 → 改列第 2 類，`day.ts` 那行註解從「回退值 0」改成「沒搬」。
9. 第 0 列職位兩邊定義不同 → 標成不可比，卡面不列。
10. `--shots` 先寫圖才驗證 → 先驗證（逐日數字＝正式對照那一列）再寫；`--shots` 只拍回退設定。

施工中另外遇到、照實記下：

- **推進一天太慢**：首版 4.68 ms，因為每天整張重算地價基準。改成只重算那一框，以整張版雜湊相同作證。
- **推進一天的守衛紅過一次**：首版取 P95，在背景有別的工作時紅過一次（平均 2.01、P95 5.00）。改成三輪取平均最低的一輪，P95 與最大值照實列出。
- **第 120 天的 2D 樣張整片白**：那天剛好暴雨打閃電。拍照那一跑把閃電計時歸零，並驗證逐日數字跟正式對照相同。
- **對照圖第一次是空白**：22 KB，拍之前少了一次頁面導航，補上。
- **煙霧新加的「卡片開著時升級」首跑崩潰**：`clean=1` 時介面沒掛進 document，`document.querySelector` 拿到 null。改從 `__gt.card()` 讀。

### 驗收

1. **決定性** ✅
   - 同種子 120 天雜湊兩次都是 `02237577`，換種子 `5bb60b18`。
   - 瀏覽器推 120 天的雜湊＝Node（煙霧）。
   - 純度守衛照舊綠。
2. **只接線** ✅（有保留）
   - 25 個行號依序出現；31 個規則函式都真的呼叫。
   - 行為面有回歸錨點與實驗線第 1 天錨點。
   - 保留：`day.ts` 裡逐棟累加人口、職位、幸福，k1 重算，每 30 天財富那一圈，都是照行號抄的接線膠水，沒有 D009 式的逐項對拍（見「沒做成的事」②）。
3. **新補的兩個場對拍** ✅
   - 252 張隨機小圖 × 三種狀態（重建、增量蓋印／撤印、換預算）逐格相等。
   - 29 個原碼單點突變都紅；1 個經證明等價：污染 `v<=0` → `v<0`，最遠一圈最小值是 3。
4. **起步城碼對帳** ✅ 路 185、分區 192／31／31、建築 2，逐格道路等級相同。
5. **歷史重播** ✅
   - 222 筆生長／升級事件重播出 194 棟，逐欄（id、種類、等級、變體、屋齡、位置、佔地、廢棄、蓋起日）相同，occ 相同。
   - 格式 1 照讀。
6. **跟實驗線對照** ✅ 量了，不判：存證、表、三項原因都在上面。
7. **畫面** ✅
   - 對照圖：第 0、30、60、120 天，左邊是實驗線回退設定、種子 5162026 的 2D。
   - 手機直式一張（第 61 天）。
8. **手機預算** ✅
   - 第 121 天：46,972 個三角形（上限 118,884）、draw call 15（上限 18）。
   - 重建一次場景：CPU 降速 6 倍下 239／159／201 ms，中位數 201（上限 400）。
   - 推進一天：平均 1.25 ms、P95 2.25、最大 4.64（上限 5）。
   - 註：重建計時量的是 JS 產生街區計畫與場景的時間；GPU 上傳與第一幀不在裡面（無頭 Chrome 走軟體算圖），手機實機沒量。
9. **上線**：Node 守衛 74 項（D010 新增 20）、煙霧 121 項（D010 新增 10）本機綠；Actions、Pages 見 LOG。

![D010 對照](img/D010-compare.jpg)

![D010 手機](img/D010-mobile.png)

### 沒做成的事

1. **整城軌跡跟實驗線不一致**：第 120 天商業 31 對 0.1、幸福 0.40 對 0.24、住宅 134 對 54。原因是第 2 類系統都沒搬：經濟快照、通勤、壅堵、垃圾、糧食、夜間城市、城市活動、火災、犯罪、廢棄、疾病、死亡。「多種子統計一致」要等之後逐層加系統的卡。
2. **行為錨點只釘到第 1 天**，8 個種子都從第 2 天分岔。
   - 沒做：審查建議的較強做法——在實驗線記憶體副本裡把第 2 類系統短路，逐步記中間值（電力、每棟幸福、需求、生長名單），拿來比 `stepDay`。
   - `day.ts` 的接線膠水沒有逐項對拍，只靠行號、第 1 天錨點和本線自己的回歸錨點。
3. **實驗線預設設定還不是可以追的目標**：起步城沒有供水設施，每天扣 0.2；限電讓工業全停。它只當參考列在表上。
4. **起步城一座電廠不夠**：約 75 棟以後新房子都沒電（本線約第 31 天，北邊先拿），工業在兩邊都斷電。卡面的佈局沒改；D011 玩家能自己蓋電廠之後再看。
5. **有電棟數兩邊差 1～3 棟，沒追到底**。
   - 實驗線那邊是每天收尾後數 `b.pw`、排除當天新生的：預設設定最多 78、回退設定最多 76；本線的容量是 75（第 100 天起 67）。
   - 實驗線的數法可能含當天被 tick 略過的建築（著火、廢棄）留下的舊旗標，所以不能據此說容量不同。
6. **2D 樣張只拍一個種子、只拍回退設定**。
7. **沒上實機**：重建計時只量 JS，手機實機的幀率、耗電、GPU 上傳都沒量。
8. **畫面只做了必要的部分**：沒有逐日動畫（新房子直接出現）；建築卡沒有「沒電」的標示。
