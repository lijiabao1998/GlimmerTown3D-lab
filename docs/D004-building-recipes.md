# D004 建築配方與街廓密度（住商工）

**基線**：`35313d4`
**參考**：2D 實驗線 `lijiabao1998/GlimmerTown-lab` @ `d23c18d`（v13.43／T638），只讀。下文行號都指 `index.html` @ `d23c18d`。
**對拍**：依 CLAUDE.md 規則 8。這張搬的是公式層，所以要**逐項相等**。

## 為什麼做這張

D003 把實驗線的城市原樣搬進 3D，但住商工只是量體佔位：每格一棟，高度量自精靈圖。跟 2D 並排，有兩個落差一眼看得出來：

1. **密度**：實驗線把相鄰的同類住商工併成超街區，一塊地畫一張大圖；3D 每格一棟，所以市中心比 2D 密。
2. **外形**：實驗線住商工的外形由原型表 `ARCHE568` 決定，包括佔地框、高度倍率、第二量體、平頂或坡頂；3D 只有四種通用盒子。

這兩件都是規則 1 說的「全局觀感」，所以這張不替業主選：三檔都做好，拍同一個畫面的對照圖。**預設畫面維持 D003 現況**，等業主挑定才改。

## 從實驗線讀到的事（原樣照搬，怪癖也照搬）

### 超街區切分

- **誰能併**（`rciMergeable547` 70454）：住商工（k 1–3）裡同一個 k 的建築，等級不必相同。例外是第一級住宅的 villa 原型，它不併（T602，`arNameVilla600` 70747）。
- **起點**（`rciBlockOrigin547` 70457）：左鄰、上鄰都不是可併的同類。
- **認領**（T633，`buildPart633` 70493）：依掃描序（y 先、x 後）。後到的街區不准伸進已被認領的格子。
- **長法**（`rciGrow633` 70477）：
  1. 先長正方形，上限 4（T602 `blockMax602` 70456）。
  2. 再往右長、再往下長，兩個方向上限都是 4。
  3. 截斷：
     - T603 聯排：住宅第一級進深 1，第二級以上進深 2；商業第一級進深 2。進深取短邊，看的是**起點那格**的等級。
     - T604：工業最大 2×2。
- **等級與變體**：`rciGrow633` 回傳的是區內最高等級與那格的變體（`maxLv`、`v`）。但繪製時拿的是**起點那格**的等級與變體（61581）。本線照抄這個怪癖。
- **截掉的格子**：沒有人畫。它們上鄰是同類，所以不是起點；也沒被任何街區認領。實驗線叫這類格子 D0「看不見的建築」，業主還沒挑檔（實驗線 T633 卡 §1）。
- **T555 吸收**（`rciAbsorbed555` 71878）：一個 1×1 起點，上下左右只要有同類鄰居屬於 ≥4 格的街區，就不畫。

### 量體（`makeBlockSprite547` 71253）

- **原型**：取 `ARCHE568[k_lv][|v| mod 表長]`（70657、70742）。
- **牆高**（像素，span＝短邊）：
  - lv1＝14＋3·span；lv2＝20＋4·span；lv3＝26＋5·span。
  - 再乘原型的 hm、四捨五入，至少 8（T601）。
- **坡高**（p＝屋脊方向的邊長）：
  - lv1＝14＋4·p；lv2＝18＋5·p；lv3＝22＋6·p。
  - 住宅改成 max(12, 8＋3·span)，商業固定 8。
  - 屋脊方向（`ridgeAxis554` 70536）：長邊比短邊多 2 以上就順長邊，否則看 v 的奇偶。
- **上層量體高**（stepH）：lv1＝10，lv2＝16，lv3＝22。
- **佔地框** `box`＝[u0,u1,v0,v1]：u 沿地圖 x，v 沿地圖 y（`paraPt559` 70584）。地圖 y 在 3D 裡對到 z。
- **T582 前後分棟**：原型沒有第二量體、街區 ≥6 格（工業 ≥4 格）、進深框 ≥0.5 時，沿進深切成前後兩棟：
  - 中間留 0.08 的巷；
  - 後棟高度 ×0.85。
- **T602 裙樓**：第二量體是整塊地、而且街區 ≥4 格時：
  - 裙樓改成 [.08,.92,.06,.38]；
  - 高度倍率不超過 0.40。
- **第二量體高**＝max(6, round(牆高 × ex.hm))。
- **屋頂分支**：
  - **立面路徑**：原型帶 `fs`，就交給英美立面繪製器（T577）。例外是 T604「一戶一尖」：進深 ≥2 的非平頂住宅不走立面。立面繪製器自己回報是不是坡頂。
  - **核心路徑**（其餘）：
    - 平頂原型：平頂。
    - 住宅：一戶一個山牆，戶數＝clamp(長邊, 2, 8)。
    - 商業：平頂加 4px 女兒牆。
    - 工業：<4 格是鋸齒屋頂，齒高照 `sawRise608` 70931；≥4 格是平頂。
- **上層量體**：核心路徑上 ≥4 格的非住宅，屋頂中央再疊一個上層量體。四邊各內縮 32%（`shrinkPara547` 70526），高 stepH。
- **顏色**：`metroPalette516(k, v, 1, lv)`（70268），再經 `speciesPal601`（70748）與 `shade`（39978）。
- **像素換成格**：跟 D003 一樣除以 39.2（D003 卡「高度換算的理由」）。

## 三檔（業主挑）

| 檔 | 名稱 | 住商工怎麼分塊 | 看起來 |
|---|---|---|---|
| A | 一格一棟 | 每格自己一塊，用 1×1 配方 | 最密 |
| B | 照實驗線 | 切分、吸收、D0 空格全照實驗線 | 最像 2D，街廓裡會有草坪空格 |
| C | 超街區補滿 | 先照實驗線切。還沒被認領的格子（D0）當成新起點，依掃描序用同一套長法補切；被吸收的 1×1 照畫 | 塊狀跟 B 相同，但沒有空格 |

- 三檔的住商工用同一套配方；非住商工不動，照 D003。
- 網址 `?blocks=a|b|c` 切換，面板上也有切換鈕。
- 不帶參數就是 D003 現況。

## 改什麼

1. **抽取**：`tools/lab-extract.mjs` 加 D004 段，無頭、只讀實驗線。輸出都存成黃金樣本，帶 commit 與行號：
   - **原型表**：`ARCHE568` 原表 → `src/content/lab-arche.json`。
   - **切分**：兩座樣本城逐格的切分結果 → `src/content/samples/d004-partition-{seed516,ai120}.json`。每格記：
     - 是不是起點；
     - 街區的寬、高、k、lv、v；
     - 起點那格的 lv、v；
     - 是否被吸收。
   - **量體**：→ `src/content/samples/d004-massing.json`。
     - 範圍：k 1–3 × lv 1–3 × 寬 1–4 × 高 1–4 × v 0–11，共 1,728 組。
     - 內容：原型名、路徑（立面名或核心）、坡頂旗標、牆高、主體框、第二量體框與高度、調色盤。
     - 取法：包住 `subPara568`、`massBox568`，讀 `__t547`。
2. **切分**：`src/content/blocks.ts`，純函式。照抄實驗線的切分，另給 A、C 兩檔的切法。
3. **配方**：`src/content/recipes.ts`，純函式。照抄量體公式、屋頂分支與調色盤，並換算成以格為單位的幾何描述。
4. **渲染**：`src/render/cityScene.ts` 依檔位畫住商工街區，包括：
   - 主體與第二量體；
   - 屋頂：平頂、女兒牆、一戶一山牆、四坡、鋸齒；
   - 上層量體、煙囪。

   點擊時取點到的那一格，建築卡照舊。
5. **工具**：
   - `tools/unit.mjs` 加對拍守衛與注入錯誤；
   - `tools/smoke.mjs` 加三檔的瀏覽器守衛；
   - `tools/shoot.mjs` 加 `d004` 對照組。

## 不改什麼

- 城市模型、解碼器、世界歷史格式都不動。切分與配方只讀城市、不改城市（規則 2）。
- 非住商工的 170 多種建築、道路、地面、樹都不動。
- 立面細節不搬：英美立面的逐戶高度、窗、店面、招牌都不搬。窗型只記在配方裡，這張不畫。
- 夜景、施工中（T635）、廢棄外觀都不做。T622 旋轉對調也不做，3D 用不到。
- 不碰任何 2D 倉庫。

## 驗收（動手前寫）

1. **原型表**：`lab-arche.json` 跟實驗線執行期的 `ARCHE568` 深度相等。
2. **切分對拍**：兩座樣本城逐格比對實驗線的 `rciBlockOrigin547`＋`rciAbsorbed555`，差異 0。
   - 比的是：是不是起點；寬、高、k、lv、v；是否被吸收。
   - Node 跑一次，瀏覽器跑一次。
   - 另外列出統計：
     - 街區數、多格街區數；
     - 住商工格分四類的格數：畫在多格街區、單格、被吸收、D0 沒人畫；
     - 重疊格數，必須是 0。
3. **量體對拍**：1,728 組逐項比對實驗線，差異 0。比的是：
   - 原型名、路徑、坡頂旗標；
   - 牆高；
   - 主體框四個分數（位元相等）；
   - 第二量體的框與高度；
   - 調色盤七個顏色字串。
4. **守衛有效**：注入以下錯誤，每一種都要讓對應守衛變紅：
   - 聯排進深 1→2；
   - 拿掉「villa 不併」；
   - 拿掉掃描序認領；
   - T582 門檻 6→5；
   - T602 裙樓上限 0.40→0.45；
   - 繪製改用街區的 maxLv。
5. **三檔都畫得出來**（瀏覽器，兩座城）：
   - **A**：每一棟建築都畫到，同 D003 守衛。
   - **B**：
     - 畫出來的住商工格，跟實驗線畫的格相同；
     - D0 格數、被吸收格數，跟黃金樣本相同。
   - **C**：每一格住商工剛好屬於一個畫出來的街區，沒有空格，也沒有重疊。
   - 三檔的非住商工都要全數畫到。
6. **點得到**：每檔、每座城各抽 20 個多格街區，點街區中心。要回到該街區裡的建築，建築卡要打得開。
7. **預設不變**：不帶 `blocks` 參數時：
   - 三角形數、draw call、畫到的建築數，都跟 D003 基線相同。基線在本輪動工前量過：種子城 79,256 個三角形、12 次 draw call；AI 城 70,910 個、12 次。
   - D003 的守衛照舊全綠。
8. **手機預算**：
   - 三檔在兩座城的三角形數，都不超過 D003 基線的 1.5 倍；
   - draw call 不超過 18；
   - 建場時間照實記進卡面。
9. **純度與素材**：
   - `src/content/*.ts` 不 import three，也不碰 DOM；
   - 零外部請求照舊。
10. **對照圖**：
    - 三個視角，每個五格：實驗線 2D、D003 現況、A、B、C。視角是種子城中景、種子城遠景、AI 城中景。
    - 另拍一張手機直式。
    - 放進 `docs/img/`，給業主挑。
11. **上線**：煙霧綠、GitHub Actions 綠、Pages 部署成功。

## 施工紀錄

2026-09-25，本機施工（雲端 session 額度用完，業主要本機接著做）。開工時 HEAD＝`origin/main`＝`ef9412b`。實驗線用 `scratch/lab-src`：從 GitHub 無 blob 複製的 `d23c18d`，工作樹乾淨。業主桌面的實驗線資料夾落後 15 個 commit，沒有 `d23c18d`，所以沒用，也沒動。

**抽取**（`node tools/lab-extract.mjs --lab=scratch/lab-src --part=d004`，約 25 秒）

- **原型表**：`ARCHE568` 共 9 組、44 個原型。靜態抽出的字面量跟執行期深度相等，才寫進 `src/content/lab-arche.json`。
- **切分**（`src/content/samples/d004-partition-{seed516,ai120}.json`）：

  | | 住商工格 | 街區（多格） | 畫在多格街區 | 單格 | 被吸收 | D0 沒人畫 | 重疊 |
  |---|---|---|---|---|---|---|---|
  | 種子城 | 924 | 191（124） | 487 | 57 | 10 | 370 | 0 |
  | AI 城 | 312 | 125（62） | 152 | 61 | 2 | 97 | 0 |

  種子城有 17 個多格街區，區內最高等級跟起點那格不同；「繪製用起點等級」這個怪癖在這座城看得到。
- **量體**（`src/content/samples/d004-massing.json`）：1,728 組。路徑分佈：
  - 核心 1,379 組；
  - 立面：usMainStreet 96、ukHighStreet 80、usPrewar 64、ukVictorian 32、ukSemi 21、usBrownstone 21、ukMansion 21、ukTerrace 14。

**對拍結果**

- 切分：兩座城逐格 0 差異，Node 跑一次、瀏覽器跑一次。統計跟實驗線相同。
- B 檔要畫的街區（起點、寬高、k、起點那格的 lv／v）：0 差異。
- 量體：1,728 組 0 差異。比了這些：
  - 原型名、路徑、坡頂旗標、牆高；
  - 主體與第二量體的框（位元相等）、第二量體高；
  - 屋頂分支：鋸齒齒高、女兒牆與上層量體的內縮、一戶一尖的戶數與坡高；
  - 七色。

**守衛有效**（注入錯誤，對應守衛變紅；還原後綠）

- 聯排進深 1→2：11 格不同。
- 拿掉「villa 不併」：90 格。
- 拿掉掃描序認領：28 格。
- T582 門檻 6→7：98 組。
- T582 門檻 6→4：138 組。
- T602 裙樓上限 0.40→0.45：22 組。
- 繪製改用街區的 maxLv：34 筆。
- **卡面寫的「T582 門檻 6→5」變不了紅，是等價突變。** 街區寬高都在 1–4，面積只有 1、2、3、4、6、8、9、12、16，沒有 5，所以 ≥5 跟 ≥6 在實驗線、在本線都分不出來。守衛改成斷言「它確實等價」，另用 6→7、6→4 驗。

**三檔的量測**（瀏覽器；建場景時間是本機，含切分 0.4–2.5 ms）

| | 三角形（對 D003 基線） | draw call | 畫到的建築 | 建場景 |
|---|---|---|---|---|
| 種子城 D003 | 79,256 | 12 | 945／945 | ~46 ms |
| 種子城 A | 89,456（1.13 倍） | 12 | 945／945 | 39 ms |
| 種子城 B | 55,056（0.69 倍） | 12 | 565／945 | 31 ms |
| 種子城 C | 64,868（0.82 倍） | 12 | 945／945 | 39 ms |
| AI 城 D003 | 70,910 | 12 | 399／399 | ~36 ms |
| AI 城 A | 72,838（1.03 倍） | 12 | 399／399 | 29 ms |
| AI 城 B | 62,734（0.88 倍） | 12 | 300／399 | 27 ms |
| AI 城 C | 66,374（0.94 倍） | 12 | 399／399 | 30 ms |

- 不帶 `blocks` 時跟基線逐位相同。
- B 檔沒畫的建築數，正好是 D0 加上被吸收的：
  - 種子城 370＋10；
  - AI 城 97＋2。
- 單檔 716.1 KB（gzip 195.9 KB），比 D003 多 19 KB。增加的是原型表、切分、配方與渲染；黃金樣本不打包。

**施工中自己抓到、照實記下的事**

1. **實驗線的函式包不住。** 主程式整段包在一個 IIFE 裡（37214–72931），`rciAbsorbed555`、`ARCHE568`、`subPara568` 都不在 `window` 上，卡面寫的「包住 subPara568」直接做不到。
   - 改法：抽取時開記憶體裡的副本，在 IIFE 收尾前插一行只讀出口 `window.__d004`（`cdp.mjs` 加 `overlay`）。
   - 不呼叫 hook 就什麼都沒包，行為跟原檔一樣。
   - 原檔不動，副本不落地。插了什麼、插在哪一行，都記在黃金樣本的 `source`。
2. **實驗線匯入後還在跑模擬。** 首跑比對城市時，每棟的 age 都多了 1。改成匯入跟 `GV.setSpeed(0)` 放在同一次同步呼叫裡。
3. **實驗線匯入分享碼後，會自己重挑住商工的變體。** 首跑切分差了 852 格，追到 T531 的 `ensureVariety531`（66845）：讀檔後依當下的密度與地價（`pickV406`）重挑 v。匯入時地價、交通這些場都歸零，挑出來的跟存檔不同：
   - 種子城 924 格有 848 格被改；
   - AI 城 312 格有 132 格被改。

   存檔的 v 就是匯出前實驗線畫面上的 v，3D 讀的也是存檔，所以抽取時把 v 還原成存檔值再切。被改的格數記在樣本的 `extra.vRepicked531`。**這是實驗線自己的行為：同一個碼匯入實驗線，外觀會跟匯出前不一樣。** 要不要處理，是實驗線的事。
4. **統計第一版把「不是起點」當成 D0**，寫成 733 格。其實 D0 是「沒有任何街區蓋到」，370 格。已改成跟 `partitionStats` 同一個格式，單元守衛直接比。
5. **點街區中心的守衛首跑紅了 1–4／20。** 斜視角下，街區中心被前面較高的建築擋住，點到前面那棟才是對的。
   - 改成從正上方點：驗的是「點到街區 → 回到街區裡那一格的建築」。
   - 斜視角直接點中的數目照樣回報：16–20／20。
6. **2D 遠景一片空城。** D003 的遠景縮放是 .45，但實驗線 z<.5 是 `lodMini`（60363），建築整個不畫、只剩路網。D004 的遠景改用 z .5，3D 對應 3.42×.5＝1.71。
7. **2D 圖上有停電閃電。** 匯入後、暫停中，實驗線的供電狀態還沒重算。試過直接呼叫它的 `computePower`，沒消掉，那段程式已拿掉。不推進模擬日去消它，因為推了城市就不是同一座。圖說寫明。

**對照圖**（`node tools/shoot.mjs --set=d004`，每格取畫面中央 800×500、1:1）

- `docs/img/D004-seed-mid-compare.png`：種子城・中景。
- `docs/img/D004-seed-far-compare.png`：種子城・遠景。
- `docs/img/D004-ai-mid-compare.png`：AI 城・中景。
- `docs/img/D004-mobile.png`：手機直式，B 檔點一個 2×2 工業街區。
- 每張五格：實驗線 2D、D003、A、B、C。每格標三角形數、draw call、畫到幾棟、街區數。

**綠燈**

- 煙霧測試 62 項（32 秒）。D004 新增 23 項，D001–D003 的照舊綠。
- Node 守衛 33 項，D004 新增 13 項，含內容層純度。

**沒做成的事**

- **業主還沒挑。** 預設仍是 D003 現況，三檔只能用網址或面板切換。
- **B 檔四成住商工格是空的**：種子城 924 格有 370 格 D0。這是實驗線切分本身的結果（T603 聯排把進深截成 1 或 2 格，截掉的格子沒人畫），實驗線 T633 卡 §1 也還沒定。C 檔就是補這個洞的做法。
- **立面細節沒搬。** 這一項照卡面「不改什麼」，並且有幾處用了本線自訂的數字：
  - 英美立面的坡頂只照它回報的旗標畫外形。坡高用本線的近似（進深 ×0.36，夾在 6–12 px），屋頂顏色是本線挑的（石板灰、陶瓦），不是實驗線的數字。
  - 窗型、店面、雨遮、女兒牆線腳、屋頂設備、前庭都沒畫。窗子沿用 D003 的窗貼圖。
- **煙囪只有大概位置與高度**，而且只畫了三種：住宅每戶一支、工業小煙囪、stack／chimneyHall 大煙囪。
- **B 檔的空格仍是 D003 的地塊鋪面**（灰邊草坪），不是 2D 那種純草坪。
- **2D 跟 3D 的取景有點偏移**，是 D003 定的縮放換算，這張沒動。
- **T531 的變體重挑沒搬。** 本線讀存檔的 v；實驗線匯入同一個碼後，外觀會自己變。
- **沒上實機量**，手機預算只量了三角形數與 draw call。
- 夜景、施工中、廢棄外觀、旋轉對調照卡面不做。
