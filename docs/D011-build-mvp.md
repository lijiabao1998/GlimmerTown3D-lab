# D011 建造 MVP：鋪路、劃區、供電，看它長

**基線**：`b641b36`
**參考**：2D 實驗線 `lijiabao1998/GlimmerTown-lab` @ `d23c18d`（v13.43／T638），只讀。行號都指 `index.html` @ `d23c18d`。
**授權**：
- 業主 2026-09-25「剩下的你繼續做吧」；D000 里程碑排定 D011＝建造 MVP（「路、區、電、成長」）。
- 業主 2026-09-26 看完 D010：「但是玩法呢」。
- 業主 2026-09-26：「數據格式能沿用的還是可以沿用，UI 這塊可以更現代化」；「按一個按鈕，裏面的數據比如 12345、11 12 13 14，還是可以這麼堆疊」。記成 CLAUDE.md 規則 9。

## 為什麼做這張

D010 之後城市會自己長，但玩家什麼都不能做。這張做出第一個能玩的循環：
- 開一座新城，手上 $3000；
- 鋪路、劃住商工、蓋電廠和警察局、拆除，每一樣都花錢；
- 按 ▶，城市照 D010 的規則長，每天收稅、付維護費；
- 錢、電、人口一眼看得到；關掉再開，城還在。

規則、數字照實驗線，跟實驗線對拍（規則 8）；按鈕和手勢本線自己設計（業主：UI 可以更現代）。

## 做什麼

### 1. 資料：照實驗線的格式與編號（規則 9）

- **編號**：建築種類 k、分區 1／2／3、道路等級 1–5、地形碼都照實驗線。這張用到的都是現成編號（1–3 住商工、5 發電廠、11 警察局），不新增。
- **存檔格式**：實驗線的分享碼格式，跟 D003 讀、D010 產生起步城碼的是同一種。
- **附加欄位**：本線的世界歷史放在附加欄位 `d3`，實驗線原有的欄位一個都不改。實驗線有先例：擴充版分享碼的 `egoal`（64902 起）。
- **兩邊互通**：3D 蓋的城匯出分享碼，貼進實驗線能開；實驗線匯出的碼，本線讀得回。

### 2. 開局

- **新城**（這張的主角）
  - 種子城的地形（地形、高地、樹），其他清空。
  - 標準難度 `df:1`、資金 $3000：實驗線預設難度的起始資金，`DIFF_MONEY[1]`（39435），也是 `newWorld` 給的數（51110）。
  - 第 1 天、種子 5162026、星等 0、里程碑 0。
  - 碼由 `node tools/lab-extract.mjs --part=d011` 產生，實驗線讀回的對帳數字要跟本線相同（同 D010）。
- **起步城**
  - D010 那一座，碼不動（`df:3` 沙盒）。
  - 照實驗線的沙盒規則：蓋東西不花錢（51505），不做每日結算（56053）。
- **我的城**：有存檔時排在最前面，接著上次繼續。
- **其他城**：種子城、AI 城、全種類照舊只能看。

### 3. 工具：規則照實驗線

放置照 `doPlace`（51626 起），判斷能不能蓋照 `canPlace`（51262 起），造價照 `placeCost`（51504 起）。

| 按鈕 | 實驗線工具 | 造價 | 寫進資料 |
|---|---|---|---|
| 路（五級可選） | `alley`／`road`／`coll`／`art`／`hwy` | 8／15／28／55／120（`ROAD_COST` 37428）；蓋在水上加橋 60（51191）；升級付差價，降級拒絕（51509–51513、51270） | 道路，等級 1–5 |
| 住／商／工 | `zr`／`zc`／`zi` | 8；已經劃區的格子改劃免費（51515）；同類重劃不動（51664） | 分區 1／2／3 |
| 電廠 | `plant` | 550 | k5，變體 `(x*7+y*13)%3`，不抽亂數（51672） |
| 警察局 | `police` | 500 | k11，變體 `ri(5)`，抽一次亂數（51687） |
| 拆除 | `doze` | 2（坑 120），不退錢（51546） | 一次拆一層，順序：建築 → 路 → 分區 → 樹……（51776–51818）；拆掉建築時分區留著 |

- **加價與拒絕**：樹上蓋東西加 $2（51623）。造價大於資金就拒絕，剛好相等可以蓋（51631）。
- **副作用照實驗線**：放置前先標地價髒框（51627）、覆蓋與污染蓋印、砍樹時的污染減免（52397），以及警察局抽的那一次亂數。
- **為什麼要有警察局**：住宅升二級需要警察覆蓋（55645）。三級要學校、供水、污染低於 15（55646、55648），這張不做，所以城市最高長到二級。

### 4. 手勢：語意照實驗線觸控（T436），介面本線自己做

- **路**
  - 按下後拖到終點，路線是先 x 後 y 的 L 形（`roadDraftTiles436` 62597）。
  - 放開手指才蓋，從起點一格一格蓋；錢中途不夠時，蓋前段、略過後段（`commitRoadDraft436` 62779）。
- **分區、拆除**
  - 拖出矩形。放開時先算總價，不夠就整塊不蓋；夠就從左上角逐列蓋（`commitRect` 62974、62996）。
  - 矩形拆除會略過二級以上的住商工；單格拆二級以上的建築要再按一次確認（62983）。
- **電廠、警察局**：點一下就蓋。
- **預覽**：拖曳時顯示每一格能不能蓋，以及總價；錢不夠時總價變紅。
- **鏡頭**
  - 選了工具：一指建造，兩指縮放、平移。實驗線的兩指不能平移（62866），本線加上。
  - 沒選工具：照舊一指旋轉。
- **滑鼠**：用同一套語意（預覽、放開才蓋）。實驗線的滑鼠是按下就蓋、自由描，本線不照搬。
- **復原 ↩**
  - 同一天之內，可以一筆一筆往回退：還原格子、全額退錢、重算覆蓋，亂數不倒回。照 T460（66570、66597）。
  - 過了一天就不能退。實驗線可以隔好幾天再退，連帶刪掉之後長出來的建築，本線不照搬。
  - 不做重做。

### 5. 資金：照實驗線每天結算（56053）

- **每天結算**
  - 稅收逐棟算，照每天建築清單的順序：住 55962、商 55963、工 55965。
  - 減掉維護費：路照 `ROAD_UPKEEP`（37429），每座電廠 4、每間警察局 4（55969–55990）。
- **結算之後依序**
  - 人口里程碑（`MILES` 37861，56081）；
  - 星等獎金（56100–56131）；
  - 紓困 $250：資金 < 20、當天淨額 ≤ 0、距上次紓困 60 天以上（56142）。
- **第 2 類系統的乘數先固定**
  - 實驗線照跑、本線沒搬的有：經濟快照、貿易進口、城市活動、夜間城市。
  - 這張把它們的乘數固定為 1、進口費為 0。
  - 差距照 D010 的做法量出來、記在卡上。
- **這張不做**：貸款、稅率調整；服務預算固定 1。
- **負債**：不會輸，只是造價大於資金時不能蓋（照實驗線）。

### 6. 世界歷史（規則 4）

- **每一格的變動記一筆事件**，只增不改，陣列順序就是真相。`g` 是同一筆手勢的編號：
  - `{day, t:'road', x, z, rc, cost, g}`
  - `{day, t:'zone', x, z, zone, cost, g}`
  - `{day, t:'place', x, z, k, lv, v, id, cost, g}`
  - `{day, t:'doze', x, z, layer, k?, id?, cost, g}`
  - `{day, t:'undo', g}`
- **拆掉的建築留墓碑**：保留編號，記拆除日。
- **格式**：`CITY_FORMAT` 2 → 3，格式 1、2 照讀。重播跟模擬用同一個套用函式。
- **卡片**：建築卡、空地卡列出這一格的事件（第幾天鋪路、劃區、蓋、拆）。

### 7. 存檔

- **什麼時候存**：自動存到 localStorage，時機是每一筆手勢之後、播放中每 5 天、離開頁面時。
- **鍵名**：用本線自己的前綴，不讀也不寫實驗線的 `glimmerville.*`。兩條線都在 `lijiabao1998.github.io` 底下，localStorage 是共用的。
- **內容**：實驗線分享碼格式的目前狀態，加上附加欄位 `d3`（歷史）。
- **讀檔語意照實驗線 `load`**：亂數用 `seed^day` 重設、天氣重設（66876、66931）。跟實驗線一樣，讀檔之後的走向會跟沒讀檔時不同。
- **選單**
  - 「匯出分享碼」：複製到剪貼簿。
  - 「貼上分享碼」：帶 `d3` 的碼可以接著蓋；其他碼照舊只能看。

### 8. 地價髒框：照實驗線，連同它的一個 bug

- **實驗線的行為**
  - 實驗線每天收尾都掛著「隔天整張重算地價基準」（64129、67355）。
  - 但 `doPlace` 第一行 `markLandDirty(x,y,20)`（51627）會把它換成 41×41 的框（53074–53084），隔天就只重算框裡。
  - 框外，前一天新工業造成的污染變化就被漏掉了。放置失敗也會這樣。
- **本線照抄**：理由是規則 8「能逐位元組相等的地方就要相等」。程式與卡面都標明這是實驗線的 bug；實驗線修了，本線跟著改。
- **回報**：這個 bug 要回報給業主，由實驗線那邊決定要不要修。

### 9. 介面

依據是業主「UI 可以更現代化」，以及規則 1 補充：Claude 自己判斷、拍前後對照圖、選擇記在卡上。

- **上方一行狀態列**
  - 顯示城名、資金、第幾天、人口、電（有電棟數／容量）。
  - ☰ 選單收進城市清單、街區 A／B／C、分享碼、300 年示範。
- **「D003 現況」鈕**：從面板拿掉（D000 已排定），`?blocks=off` 網址照留給守衛用。
- **下方工具列**
  - 一列：播放、速度、復原。
  - 一列：工具（路、住、商、工、電、警、拆）。
  - 圖示用程式畫（規則 7）。
- **開局提示**：照實驗線教練列的順序（66641–66644）：先鋪路 → 路邊劃住宅 → 蓋電廠接到路 → 按 ▶。
- **電不夠**：提示再蓋一座電廠。一座電廠約供 75 棟（52473、55008）。
- **開局暫停**：按 ▶ 才走，同 D010。

## 不改什麼

- **D009、D010 的公式**：不改。D010 的 8 種子 × 120 天回歸錨點不變：起步城是沙盒、不結算；沒有編輯的日子，地價照舊等同整張重算。
- **只能看的城**：兩座樣本城、全種類、300 年示範照舊只能看。
- **實驗線**：只讀。
- **這張不做**：學校、供水、三級建築、公園、電線、貸款、稅率、火災、犯罪……留給之後的卡。

## 驗收（動手前寫）

1. **建造規則對拍（公式層）**
   - **黃金樣本**：實驗線本卡工具的 `canPlace`、`placeCost`、`doPlace`，加上 L 形路線、`commitRect`、拆除、復原，把原始碼放進 Node 跑。
   - **案例**：≥ 200 張隨機小圖，每張 ≥ 20 筆操作。要涵蓋：拒絕、錢剛好夠／差一塊、樹、水上橋、升級與降級、改劃、各層拆除、二級以上的拆除規則、復原。
   - **逐筆比**：成功與否、拒絕理由、資金（完全相等）、格子欄位、覆蓋與污染場、亂數抽取紀錄、地價髒框。
   - **突變**：注入 ≥ 20 個原碼單點突變都要紅。
2. **資金對拍（公式層）**：稅、維護費、結算後段（里程碑、星等、紓困）的實驗線原碼，≥ 2,000 組隨機輸入逐項相等；突變要紅。
3. **實驗線實跑錨點**
   - **設定**：新城碼 × 8 個種子，回退設定（同 D010）。
   - **操作**：同一串操作分三段：開跑前一批、推進第 1 天、推進後一批。實驗線用它自己的手勢函式做，本線用本卡的函式做。
   - **兩批操作之後**：逐格、資金（完全相等）、各種場都相等。
   - **第 1 天那一列**：D010 的 21 欄加有電棟數，逐項相等。
   - **第 1 天的資金**
     - 把實驗線當天的第 2 類乘數與進口費代進本線公式，收入、維護費、結算後資金都要跟實驗線完全相等。這證明公式和輸入都對。
     - 本線實際跑（乘數 1、沒有進口）的差額照實列出。
   - **第 2 天以後**：只量不判。列均值 ± 標準差與差距（含資金），並記第一個分岔日。
4. **分享碼互通**
   - 本線蓋完匯出的碼，實驗線讀回的對帳數字（路、分區、建築、道路等級、資金、天數、難度）要等於本線。
   - 實驗線照同一串操作蓋完匯出的碼，本線讀回要等於實驗線自己的數字。
   - 附加欄位 `d3` 不影響實驗線讀回的結果（有、沒有都一樣）。
5. **決定性與歷史**
   - 同一個碼、同一串操作、同樣的天數，兩次雜湊相同。
   - 事件只增不改。重播（匯入加全部事件，含拆除、復原）出的城市等於模擬結束時的城市，逐欄相同：墓碑、路、分區、樹、occ 都要比。
   - 格式 1、2 照讀。
   - 存檔再讀檔：城市、資金（照實驗線取整）、天數、難度、歷史都相同。同一份存檔讀兩次，之後推進的雜湊相同。
6. **地價髒框**：另寫一個照實驗線逐字計算的慢速版；在帶編輯的隨機劇本上，本線的地價狀態機跟它逐格相同。
7. **介面與手勢**（煙霧測試，無頭 Chrome，用真的觸控事件）
   - 一指拖出路線，放開後蓋出來，扣的錢等於預覽的總價；一指拖的時候鏡頭不動；兩指捏合會縮放，兩指拖會平移。
   - 矩形分區錢不夠時整塊不蓋；拆除、復原都對；沙盒不扣錢。
   - 「D003 現況」不在面板上，`?blocks=off` 還能用。
   - 手機直式 412×860：狀態列、工具列、選單都在畫面內，按鈕 ≥ 44 px。
   - 存檔：重新整理頁面後「我的城」還在，內容相同。
8. **手機預算**
   - 放開手指到畫出結果：CPU 降速 6 倍下 ≤ 400 ms（三次取中位數）。
   - 拖曳中每次更新預覽（算格子與預覽的 JS，不含畫一幀）：CPU 降速 6 倍下 ≤ 16 ms。
   - 推進一天（含結算）≤ 5 ms。
   - 劇本城第 120 天：三角形 ≤ 118,884；draw call ≤ 18，拖曳預覽時多 1。
9. **畫面**
   - 介面前後對照，手機、桌機各一張；選了什麼、為什麼，寫在卡上。
   - 手機上從空地到第 120 天的建造過程一組。
   - 同一串操作，實驗線 2D 與 3D 在第 0、30、60、120 天的對照。
10. **上線**：Node 守衛、煙霧測試、Actions、Pages 都綠；D010 回歸錨點照舊綠。

## 施工紀錄

雲端施工，2026-09-26。開工時 `origin/main`＝`e5fe619`（本卡），HEAD 一致、工作樹乾淨。施工中兩個存檔提交只推在 session 分支（`313a8de`、`9d5fda5`），收工提交一起快進到 `main`。

公式層和黃金樣本交給一個多代理工作流並行做（建造規則、資金公式、開局碼三路，各配一位對抗驗證者），第一次被用量上限打斷，重跑後三路都通過驗證；驗證者提的兩個低嚴重度涵蓋缺口，另開一輪補上（見「審查」）。

### 做了什麼

1. **資料照實驗線（規則 9）**
   - 按鈕寫進資料的東西都是實驗線的編號：路 `alley`／`road`／`coll`／`art`／`hwy`＝等級 1–5，分區 1／2／3，電廠 k5、警察局 k11。沒有新增任何編號。
   - 存檔就是實驗線的分享碼，本線的世界歷史放在附加欄位 `d3`（`{f:格式, s:起始碼, g:下一筆手勢編號, hv:歷史存法, r:歷史}`，存法見「卡面更正」5）。實驗線讀帶 `d3` 的碼，讀回的數字跟不帶的完全一樣（開局碼兩張、8 個種子施工後的碼 8 張，都實測）。
   - 注意：實驗線自己存檔時會重組存檔、丟掉 `d3`。所以本線的碼拿去實驗線再存一次，回來就只剩存檔、沒有本線歷史，只能看不能接著蓋。
2. **建造規則**（`src/sim/rules/build.ts`，356 行）
   - 照實驗線 `canPlace`（51262 起）、`placeCost`（51504 起）、`doPlace`（51626 起）搬本卡工具那幾支，拒絕理由原文照抄。
   - 觸控手勢照實驗線：拉線 `commitRoadDraft436`（62779）、框選 `commitRect`（62974）、點一下（62918）、交易與復原（62725、66594）、復原堆疊上限 40（62731）。
   - 本卡以外的工具一律丟「D011 未搬」，不會默默照錯的規則蓋。
3. **資金**（`src/sim/rules/money.ts`，316 行）：稅（55960–55965）、維護費（55969–56027）、城市活動取整（56028）、結算（56053）、里程碑、星等、紓困（56081–56145），運算順序不動。
4. **施工整合層**（`src/sim/edit.ts`）：手勢 → 規則 → 每一格一筆事件 → 城市模型；同一天可以一筆一筆復原。
   - 每一筆扣的錢＝事件記的造價，依施工順序逐筆減（守衛逐位核對）。
   - 拆多格建築時如果點的是附屬格，造價記在那棟建築的拆除事件上（初版記成 0，施工中抓到、修掉）。
   - 施工改到覆蓋、污染的那一帶，打本線的逐格地價標記（半徑 20，涵蓋本卡工具的蓋印半徑：警察 10、電廠污染 6、樹 2）。為什麼要有、守衛怎麼核對，見「卡面更正」6。
5. **逐日推進**（`src/sim/day.ts`）
   - 地價髒框照實驗線，連同第 8 節的 bug 一起照抄（見「回報給業主」）。
   - 每天結算接在生長之後，照 tick() 的順序；沙盒照算收支、不入帳，里程碑與星等獎金照給（實驗線也是）。
   - 新增只給對拍用的 `class2` 選項：把實驗線那一天的第 2 類乘數、進口費、其他收入代進本線公式。平常不傳，等於乘數 1、沒有進口。
6. **世界歷史格式 3**（`src/sim/city.ts`、`src/sim/replay.ts`）：鋪路、劃區、放置、拆除、復原都記成事件，拆掉的建築留墓碑（保留編號、記拆除日）。重播跟模擬逐欄相同；格式 1、2 照讀。
7. **存檔**（`src/io/save.ts`）：localStorage 鍵 `gt3d.v1.save`（不讀不寫 `glimmerville.*`）；每一筆手勢之後、播放中每 5 天、暫停時、換城之前、離開頁面時存。讀檔照實驗線 `load`（亂數 `seed^day` 重設、天氣重設），再用 `d3` 重播歷史、跟存檔逐格核對，對不上就退回只用存檔並講原因。
   - `d3` 是外來輸入（分享碼誰都能改）：每一筆事件逐欄驗型別與範圍，驗過才進城市。
   - 存不成要講：超過分享碼上限不寫；空間滿、被封鎖都跳通知，狀態列掛「⚠ 未存檔」。開頁讀不出存檔時，原檔另存 `gt3d.v1.save.bad`、先開新城。
8. **開局碼**（`tools/lab-extract.mjs --part=d011`）：新城（種子城地形、df 1、$3000、第 1 天、種子 5162026、星等 0、里程碑 0）與預建城（對拍的拆除劇本用）。兩張都經實驗線 `GV.importCode` 讀回，對帳數字、道路等級、資金、難度、星等、里程碑、種子、城名、住宅密度與財富都跟本線相同。
9. **介面**（`src/ui/buildUi.ts`、`src/ui/icons.ts`、`src/render/preview.ts`、`src/cityView.ts`）
   - 上方狀態列：城名、第幾天與住商工、資金、人口、電；☰ 選單收城市清單（我的城、新城、起步城、種子城、AI 城、全種類）、分享碼匯出／貼上、街區 A／B／C、300 年示範。「D003 現況」鈕拿掉，`?blocks=off` 照留。
   - 下方兩列：播放、三檔速度、第幾天、復原；七個工具（路、住、商、工、電、警、拆），路另有五級可選。圖示用程式畫的 SVG（規則 7）。
   - 拖曳時逐格預覽（一個 InstancedMesh，能蓋的上工具色、不能蓋的紅）＋總價標籤，錢不夠時變紅；放開才蓋。
   - 開局提示照實驗線教練列的順序（66641–66644），本線多一步「按 ▶」；電不夠時提示再蓋電廠。
   - 開頁：有存檔就接著「我的城」，沒有就開新城並立刻存成「我的城」；開局暫停。
10. **對拍與存證工具**：`tools/d011-parity.mjs`（實驗線實跑，錄 `d011-lab.json`／`d011-3d.json`；`--shots` 拍 2D 樣張）、`tools/d011-parity-lib.mjs`（兩邊共用的量法：同一段原始碼，實驗線頁面裡 eval、本線 Node 裡 `new Function`）、`tools/d011-ops.mjs`（操作劇本）。

### 介面選擇（規則 1 補充：Claude 自己判斷、拍前後對照、記在卡上）

對照圖：`docs/img/D011-ui-compare.jpg`（起步城第 1 天，手機直式與桌機，D010 → D011）。

| 項目 | 選了什麼 | 為什麼 |
|---|---|---|
| 版面 | 上方一行狀態列＋☰ 選單；下方播放列與工具列 | D010 的面板把七顆城市鈕、三顆街區鈕、兩顆功能鈕都攤在畫面上，手機上佔掉三分之一。建造要常用的是工具和播放，其餘收進選單 |
| 工具列 | 七格等寬、圖示加字加價錢 | 手機一排放得下；價錢直接看得到，不用點進去 |
| 路的等級 | 選「路」之後多一排五格 | 最常用的支路是預設；五級並排、每格顯示價錢 |
| 按鈕大小 | 全部 ≥ 44×44 px | 手機觸控的常用下限；煙霧逐顆量 |
| 手勢 | 選了工具：一指建造、兩指縮放與平移；沒選工具：一指轉鏡頭 | 實驗線觸控的兩指不能平移（62866），本線加上，否則選著工具時沒辦法移動畫面 |
| 滑鼠 | 跟觸控同一套（預覽、放開才蓋） | 實驗線的滑鼠是按下就蓋、自由描；兩套語意並存容易誤蓋，本線不照搬 |
| 電 | 狀態列顯示「要用電的住商工／電廠容量」，超過變紅 | 卡面原寫「有電棟數／容量」。實驗線當天新長出來的房子一律帶電（55623），隔天才照容量分配，所以有電棟數會短暫超過容量（實拍出現過 153／150），玩家看不懂。改顯示需求，提示裡再說有幾棟沒電 |
| 提示 | 通知疊在上方、提示條在工具列上方 | 通知幾秒就淡出；提示條只在該做下一步時出現 |

### 卡面更正

1. **第 4 節「錢中途不夠時，蓋前段、略過後段」不精確。** 實驗線 `commitRoadDraft436`（62779）每一格各自比「造價 > 資金」。錢不夠之後，後面如果有更便宜的格（例如過了橋回到陸地），照樣會蓋。本線照實驗線；黃金樣本裡有 30 條這樣的線。
2. **第 9 節「電（有電棟數／容量）」改成「要用電的住商工／容量」**，理由見上表。
3. **第 6 節「重播跟模擬用同一個套用函式」不對**（審查抓到）。模擬是 `build.ts` 的 `doPlace` 改格子、`edit.ts` 比對前後同步城市模型；重播是 `replay.ts` 另外一份照事件改城市模型。兩份實作靠守衛綁在一起：每一筆施工、每一天之後重播都要等於模擬（見「審查」守衛那一半）。
4. **第 6 節的復原事件少寫一欄**：實際是 `{day, t:'undo', g, refund}`（退回的錢）。
5. **第 7 節存檔：歷史的存法改了一次**（規則 4：格式一改就加版本號、舊檔要能讀）。
   - 第一版把每筆事件存成物件（`d3.h`，每筆約 59 字元）。審查發現存檔沒有上限，長到分享碼上限 2,000,000 字元就讀不回來。
   - 改成 `d3.hv` 2：`d3.r` 每筆一列 `[種類碼, 距上一筆的天數, 欄位…]`，手勢編號也存差值，每筆約 18 字元。120 天劇本城的存檔 80,536 → 37,436 字元；照這個密度，上限約可存 8 萬筆事件。
   - 第一版的存檔照讀，讀回的歷史逐位元組相同；比這一版新的存法不猜，退回只用存檔。
   - 超過上限時：自動存檔不寫、提示玩家；匯出給不含歷史的碼。**歷史真的長過上限之後怎麼辦（分段、改存別處、或截斷）要業主定**：截斷會違反規則 4「只增不改」，所以本卡沒做，見「沒做成的事」。
6. **第 8 節地價：本線多一個逐格標記**（施工紀錄 4 第三點）。本線的「整張重算」只算有標記的格，省時；成立的條件是「沒有標記的格，地價基準都等於現算的值」。實驗線的 bug 讓施工後隔天一定走框、框一定蓋得到施工改過的格，所以目前少了這個標記也看不出差別（審查確認過）；實驗線修掉那個 bug 時就全靠它。守衛改成直接核對那個條件，不靠框。

### 跟實驗線對照

樣本用實驗線 `d23c18d` 重錄過（`node tools/d011-parity.mjs`，300 秒）：審查之後劇本改了（見「審查」），第一次錄的樣本作廢。

**逐位相等的部分（守衛，每次 CI 都重算本線那一半）**

- **新城兩批操作**：新城碼 × 8 個種子（`STARTER_SEEDS`），逐筆全等，**8 × 47＝376 筆**。
  - 開跑前一批 36 筆：鋪路、升級付差價、降級被拒、劃區、改劃免費、同類重劃不動、跨路被拒、電廠、警察局、同格再放被拒、水上被拒、砍樹、再砍被拒、橋、錢只夠兩格、框選錢不夠、差一塊、剛好夠、快速路再復原、次幹道、電廠再復原、警察局再復原、快速路過河（留著，路碼 4）、最後一座警察局（留著）。
  - 推進一天。
  - 推進後一批 11 筆：資金設回 3000、兩邊亂數對齊成 `mulberry32(20260926)`、找第 1 天長出來的第一棟住商工（8 個種子都找到）、拆它兩次（先拆建築、再拆分區）、警察局（抽亂數，變體兩邊逐位相同）、框選拆除再復原、重劃、拆電廠再復原。
  - 每一筆之後比：資金（不取整）、亂數抽取數、逐格投影、覆蓋與污染場、地價基準與地價（審查後加）、地價髒框、變了哪些格。單格拆除兩邊都走玩家那條路：實驗線 `commitRect`（62974），本線框選。
  - 註明「不會改到東西」的 7 筆（被拒的那些），守衛逐筆核對真的沒改；其餘每個種子都真的改到東西。
- **推進第 1 天之後的快照**：格子、場（含地價基準）、髒狀態都相等。第 1 天開頭的地價框重算，8 個種子都改了 432 格、兩邊相同。
- **第 1 天的亂數**：天氣、生長洗牌（55605）、擲骰（55618）、變體（55622）逐行次數相同。實驗線多抽的只有三行，都在生長之後：起火（55772）、犯罪（55817）、生病（55850）的擲骰，都是本線沒搬的第 2 類系統；守衛用本線推進後的格子算出這三行該抽幾次，跟實驗線逐種子相等（8 個種子多 4–6 次）。
- **第 1 天那一列**：D010 的 21 欄＋有電的住商工棟數，8 個種子逐項相等。有電棟數這一欄必然相等（第 1 天的住商工都是當天長的，生出來就帶電，55622），真正的供電對拍在預建城。
- **第 1 天的資金**：把實驗線當天探針讀出的第 2 類值（`goodsMul284` 1.25、`commerceSalesMul481` 0.804、`industrialMarketMul481` 0.78、鋼材進口 $9……）代進本線公式，8 個種子的收入、維護費、結算後資金都逐位相等。本線實際跑（乘數 1、沒有進口）比實驗線多 $9.26 ± 0.27。
- **預建城 × 8 個種子**（`d011-prebuilt.code.txt`，15 棟：含 2×2 體育場、二級、三級）：拆除劇本 11 筆逐筆全等。
  - 點體育場的附屬格整棟拆；二級第一次只預備、1 秒後再按才拆；三級隔 3.001 秒、剛好 3 秒都重新預備，隔 2.999 秒才拆；框選一級＋二級只拆一級；復原；拆小巷接支路那一格。
  - 實驗線讀檔時會重挑 11 棟住商工的外觀變體（T531，67035 → 66848–66862），本線讀檔沒有這一步；對拍工具先在本線照做同一個重挑、棟數相同（見「回報給業主」）。
  - 推進一天之後，在第 2 類系統起作用之前就定案的部分都相等：推進前就在的住商工逐棟有沒有電（每個種子 9 有電、1 沒電）、住商工以外的格、覆蓋、樹的污染減免、地價基準、地價。
  - 住宅幸福的差逐位等於三項第 2 類：垃圾容量池 −0.15（55270–55274）、離垃圾場太遠 −0.045、糧食 +0.0275（55414–55419）。全城幸福 0.5575 對 0.39，需求跟著不同，8 個種子裡 3 個推進後整張全等，其餘 5 個生長或升級不同（只量不判）。
- **分享碼互通**：
  - 本線 B 段之後匯出的碼、本線的城市模型，都等於實驗線在頁面裡自己量的城（路、分區、建築、道路等級、資金、天數、難度、星等、里程碑）。
  - 實驗線照同一串操作蓋完匯出的碼，本線讀回＝實驗線自己的對帳數字與道路等級。
  - 錄製時實驗線讀回的事實：帶 `d3` 與拿掉 `d3` 的同一張碼讀回完全一樣；兩張碼的雜湊在 Node 重算、跟現在算出的一致。

**第 2 天以後：只量不判**（8 個種子，均值 ± 標準差；第 N 天＝推進 N 次之後，同 D010）

| 天 | 指標 | 本線 | 實驗線（回退設定） | 差 |
|---|---|---|---|---|
| 30 | 人口 | 372.3 ± 16.2 | 274.1 ± 20.1 | 98.1 |
| 30 | 幸福 | 0.551 ± 0.030 | 0.154 ± 0.024 | 0.397 |
| 30 | 住／商／工 | 46.8／17.8／14.9 | 38.5／0.3／25.8 | 8.3／17.5／−10.9 |
| 30 | 資金 | 5,038 ± 335 | 4,373 ± 233 | 665 |
| 60 | 人口 | 542.1 ± 45.8 | 364.8 ± 19.2 | 177.4 |
| 60 | 幸福 | 0.512 ± 0.017 | 0.157 ± 0.022 | 0.356 |
| 60 | 住／商／工 | 57.9／21.6／19.9 | 49.9／0.3／30.5 | 8.0／21.4／−10.6 |
| 60 | 資金 | 8,071 ± 207 | 5,437 ± 525 | 2,634 |
| 120 | 人口 | 648.5 ± 34.7 | 385.8 ± 31.3 | 262.8 |
| 120 | 職位 | 338.8 ± 18.4 | 157.9 ± 58.3 | 180.9 |
| 120 | 幸福 | 0.505 ± 0.019 | 0.186 ± 0.016 | 0.318 |
| 120 | 住／商／工 | 58.5／21.8／23.4 | 52.0／0.3／22.4 | 6.5／21.5／1.0 |
| 120 | 二級（住＋商＋工） | 23.9 ± 3.8 | 0.0 ± 0.0 | 23.9 |
| 120 | 資金 | 13,756 ± 462 | 7,667 ± 894 | 6,088 |
| 120 | 有電棟數 | 103.6 ± 1.5 | 74.6 ± 9.3 | 29.0 |

- **分岔日**：8 個種子都是第 2 天（第 1 天那一列逐項相等）。
- 完整逐日數字（23 欄）在 `src/content/samples/d011-lab.json`、`d011-3d.json`；對照圖 `docs/img/D011-compare.jpg`（第 0、30、60、120 天，第一個種子）。

**差距的原因**（跟 D010 同一批第 2 類系統，D010 卡「差距最大的三項」有逐項實跑歸因）

1. **商業幾乎不長**（0.3 對 22 棟）：實驗線的經濟快照 T481／T482 第 1 天收尾就就緒，沒有二級商業時零售利用率是 0，商業需求被壓到 −0.63～−0.73，低於 −0.5 就停長（55609）。
2. **幸福**（差約 0.32–0.40）：垃圾、糧食、通勤、喪事、壅堵、夜間城市這些實驗線照跑、本線沒搬的扣分；預建城那一天逐位拆出了垃圾與糧食三項（上面）。
3. **二級**（23.9 對 0）：升級要有警察覆蓋、住宅幸福 > .45（55645 起）。審查後的劇本留了一座警察局，本線的住宅幸福約 0.5，會升；實驗線被第 2 類壓在 0.15–0.19，一棟都升不上去。
4. **資金**：
   - 第 1 天就差 $9.26：實驗線的鋼材安全庫存在企業回退（`__noEnterprise489`）時把沒有的產業利用率當 1（39574），沒有鋼廠也進口鋼材（第 1 天 $9），第 2 天起（實驗線的 day 3）工業稅再乘 1.12（`steelTaxMul`）；另外商業、工業稅有 `goodsMul284` 1.25、`commerceSalesMul481` 約 0.8、`industrialMarketMul481` 0.78。
   - 之後差距主要來自商業稅（本線 22 棟商業）、二級建築的稅，以及里程碑、星等獎金（人口多、星等高）。
5. **工業**：前 60 天本線較少，是因為本線工業需求整段偏低（−0.64～−0.69），實驗線工業需求卡在 −0.235（經濟快照的工業市場那一支）；到第 120 天兩邊接近（23.4 對 22.4，實驗線各種子差很大，± 8.2）。

### 回報給業主（實驗線的事）

1. **地價髒框的 bug**（卡面第 8 節，本線照抄）：每天收尾掛著「隔天整張重算地價基準」（64129、67355），但 `doPlace` 第一行 `markLandDirty(x,y,20)`（51627）會把它換成 41×41 的框（53074–53084）。框外、前一天新工業造成的污染變化就漏掉，要等下一次整張重算；放置失敗也會這樣。實驗線修了，本線跟著改（黃金樣本裡有一個「修掉 bug」的突變，會讓守衛轉紅，所以改的時候要一起更新樣本）。
2. **鋼材安全庫存**：企業回退時，沒有鋼廠、船廠的城市也進口鋼材當安全庫存，之後工業稅一直 ×1.12（55305、55364–55367、39574、55323）。看起來像回退路徑沒顧到的副作用，請實驗線判斷。
3. **不是 bug、提醒一下**：`commitRoadDraft436` 每一格各自比錢，錢不夠之後更便宜的格照樣蓋（上面「卡面更正」1）。
4. **框選拆除蓋到多格建築，顯示的價錢比實扣的多**：拖曳時的總價（62610–62627）和放開時的預估（`commitRect` 62980–62996）都是逐格加 `placeCost`，一座 2×2 體育場整座框起來顯示 $8、門檻也是 $8，實際只拆一次、扣 $2。本線照實驗線（預覽、門檻都是 $8）；守衛的「預覽＝實扣」在這個情形照實驗線是不相等的，所以劇本沒有框整座多格建築（見「沒做成的事」）。
5. **讀檔會重挑外觀變體**（T531，`load` 67035 → `ensureVariety531` 66848–66862；每個世界第一次 tick 也跑一次，54946）：依當下的街區密度與地價重挑每棟住商工的 `v`。只影響外觀、不抽亂數，但同一座城每讀一次檔，外觀就可能變。本線讀檔保留存檔裡的 `v`（見下面「要業主定的事」3）。
6. **D0「看不見的建築」在建造模式下更顯眼**：實驗線的切分沒畫到的住商工（D0）與被吸收的 1×1（T555）不畫、畫成草坪，本線預設 B 檔照實驗線（D004、D005）。建造時玩家看著城市長，差別就明顯了：對照那一跑（種子 5162026）第 61 天 103 棟住商工有 51 棟沒畫，第 121 天 105 棟有 65 棟沒畫。實驗線 T633 卡 §1 這件事還沒挑檔。

### 要業主定的事（本線）

1. **歷史長過分享碼上限怎麼辦**：緊湊列之後大約 8 萬筆事件會碰到 2,000,000 字元。選項：分段存（例如舊的歷史另存一個鍵）、改存 IndexedDB、或截斷（違反規則 4）。現在是停止自動存檔並提示匯出。
2. **推進一天的手機預算**：驗收 8「推進一天（含結算）≤ 5 ms」沒寫量法；照 D010 的量法（桌機）是 1.0–1.3 ms，達標。但用手機代理（CPU 降速 6 倍）量是 11–14 ms，照規則 5「桌機跑得動不算數」就是沒達到（見「沒做成的事」）。要壓到 5 ms 得把供電、結算、tick 索引這些「照實驗線每天整張掃」的部分改成增量，建議另開一張效能卡；或者把這條預算改成手機代理下的數字。
3. **讀檔要不要照實驗線重挑外觀變體**（上面「回報」5）：照搬的話，歷史重播也得能重現「讀檔時重挑」（加一種事件、或讀檔後統一重挑再核對），否則每次讀檔重播都會對不上。本卡沒搬，對拍工具在預建城那段先照做再比。
4. **D0 看不見的建築要不要畫**（上面「回報」6）：C 檔（D004 已做好）會把 D0 當新起點補畫、被吸收的也照畫；要不要改預設、或等實驗線 T633 挑檔，由業主定。

### 審查

施工完（`b96e805`）做了一輪多視角審查：整合、跟實驗線的忠實度、介面、守衛四個視角各一位審查者找問題，每一條再由另一位獨立驗證者重現（無頭 Chrome 實跑、或在副本裡做突變看守衛紅不紅）。

- **找到 45 條，45 條都重現成立**。去掉重複（同一個問題被兩三個視角各找到一次）剩 39 條：阻斷 1、重大 11、次要 24、資訊 3。
- 審查者、驗證者都只讀本線；突變全做在副本裡。

**src 的修正**（`b4ebb5f`、`1efb007`）

| 嚴重度 | 問題 | 修法 |
|---|---|---|
| 阻斷 | 從選單換城，舊城被存進新城的存檔槽：「我的城 → 起步城 → 我的城 → 種子城」之後，我的城變成起步城沙盒（$99,999,999）；離開我的城時最近幾天的進度沒存；重選我的城會退回舊檔。原因：`openSample` 先改 `sampleId` 才載入，載入時暫停順手存檔，存檔判斷看的是新的 `sampleId`、存的卻是舊城 | 「要不要自動存檔」改成載入時決定、跟著那座城走；換城、貼碼之前先用舊城自己的身分存一次；載入時只停、不存；已經在我的城又選我的城＝存一次就好，不重讀 |
| 重大 | 存放型 XSS：分享碼 `d3` 裡的事件欄位（例如把 `day` 換成一段 HTML）會被畫進建築卡的 `innerHTML`，還會跟著自動存檔一直留著；實驗線版本字串也是同一個洞 | 建築卡、選單、狀態列一律用 `textContent`；讀檔時 `d3` 每一筆事件逐欄驗型別與範圍（整數、座標在圖內、圖層與種類在清單內），不合格就退回只用存檔並講原因 |
| 重大 | 存檔沒有上限：歷史長到分享碼超過 2,000,000 字元（實驗線 64904 與本線的解碼上限）就讀不回來，開頁直接丟例外，我的城救不回來；免費改劃可以很快灌到上限 | 歷史改用緊湊列存（`d3.hv` 2，見「卡面更正」5），同一座城存檔約短一半、歷史短 3.2 倍；存檔前先量長度，超過就不寫並提示；開頁讀不出存檔時原檔另存 `gt3d.v1.save.bad`、開新城，不丟例外；匯出超過上限時給不含歷史的碼並講明 |
| 重大 | 點在狀態列的字、工具列的空隙、提示條、通知上會穿到地圖，在看不見的格子上蓋東西（實測在工具列底下蓋了一座 $550 電廠） | 狀態列、下方整塊、通知都接住觸控 |
| 重大 | 選著工具時兩指縮放：第一指落在地圖外、或抬起一指再放回去，照樣蓋了一條路（實驗線第二指一落下就取消，62787–62799） | 畫布追蹤每一根按著的指標：第二根一落下就取消施工，全部放開之前不開新的一筆 |
| 重大 | 路等級那一排的樣式寫成 `.sub`，也套到建築卡與分享碼對話框的副標題；360×740 上對話框的「取消」被播放列蓋住，匯出對話框關不掉 | 樣式只套 `#roadSub`；對話框、建築卡疊在介面之上 |
| 重大 | 播放中每天重建播放鈕的圖示與路等級的五顆鈕，滑鼠按下與放開之間節點被換掉，10 倍速時點暫停 0／10 次成功 | 只在狀態真的改了才換圖示；五顆鈕建一次，之後只切換樣式與文字；狀態列的數字也改成只改文字 |
| 次要 | 讀檔時歷史接不回來，開頁照樣說「已接著上次的城繼續」；貼碼那條路完全不講 | 看有沒有重播成功，不看提示字首；兩條路都講原因 |
| 次要 | 網址 `?sample=newcity` 不問就蓋掉我的城 | 跟選單一樣先問 |
| 次要 | 自動存檔失敗（空間滿、被封鎖）沒有任何提示 | 跳通知，狀態列掛「⚠ 未存檔」直到存成 |
| 次要 | 讀檔後過第一天之前人口顯示 0（實驗線 `load` 也不重算，模擬照它） | 模擬不動，顯示改成「—」 |
| 次要 | 路的「點一下」手指抖 6 px 跨過格線就蓋兩格（實驗線要動超過 8 px 才跟手，62884–62885） | 照實驗線；框選照實驗線立刻跟手 |
| 次要 | 狀態列的資金四捨五入，會顯示一個其實花不起的數（實驗線往下取整，64849）；−0.4 顯示成「−$0」 | 往下取整，負號跟著取整後的值 |
| 次要 | 總價標籤被工具列蓋住 | 標籤疊在工具列之上，並收在工具列上緣以上 |
| 次要 | 360 寬的手機上七顆工具鈕 43.4 px；對話框按鈕 34 px | 窄螢幕縮小間距（45.7 px）；對話框按鈕、卡片關閉鈕 ≥ 44 px |
| 次要 | 兩指在工具列上捏合，整頁被放大 | 狀態列、下方整塊不讓瀏覽器縮放整頁 |
| 次要 | 選單開著時鍵盤快捷鍵照樣在後面換工具、播放；Esc 關不掉選單和對話框 | 選單、對話框開著時 Esc 只關它、其他鍵不作用 |

（守衛那一半的修正與突變驗證，最後一輪跑完後補在下面。）

### 施工中遇到、照實記下

- **工作流被用量上限打斷**：第一次三路代理做到一半停下，留下半成品的 `build.ts`、`money.ts`。重跑時要它們逐行重審半成品；建造那一路回報沒有邏輯要改，只補了註解與行號；資金那一路重寫、沿用既有的名稱和形狀。
- **拆多格建築的造價記錯**：點附屬格拆整棟時，事件造價記成 0（錢照扣）。整合守衛的「扣款＝事件造價」抓到，改成記在第一個碰到它的格。
- **沒蓋成卻沒有理由**：同類重劃（實驗線 51664 回傳 false 但沒有理由字串）、降級被拒時，介面不知道要說什麼。改成從預覽推出理由（「已經是這一區」「已為同級或更高級道路」）。
- **煙霧測試的觸控**：
  - `Input.synthesizeTapGesture` 受實際視窗大小限制，手機版下方的按鈕會回報座標超界，改用 `dispatchTouchEvent` 按下＋放開。
  - 同一個分頁連續換頁很多次之後，Chrome 的觸控模擬不再把觸控事件送進頁面（`elementFromPoint` 是畫布，頁面卻收不到任何 pointer／touch 事件；單獨重現不出來）。煙霧 D011 段改成三個 Chrome 各跑一段。根本原因沒追到。
  - 清掉存檔後直接換頁，舊頁面離開時 `visibilitychange` 又把城存回去；改成先到只能看的樣本城清存檔。
- **播放鈕只有 40 px**：`index.html` 裡 300 年示範的 `#play { width: 40px }` 用 ID 選擇器蓋掉了建造介面的 44 px，加更具體的選擇器。
- **對照圖差一天**：實驗線拍照那一跑的天數標籤、本線參考跑的天數各錯一天（「第 N 天」到底是推進 N 次還是 `day`＝N），都改成 D010 的算法（推進 N 次之後），並加上「瀏覽器重演的雜湊＝Node」核對。
- **合成圖下方重複一截**：截圖視窗比內容高、或比無頭 Chrome 的螢幕寬時，下方會再畫一次上面的內容。改成把視窗設成內容大小再拍，介面前後那張改成兩欄。

### 驗收

（最後一輪全部跑完後填）

### 沒做成的事

（最後一輪全部跑完後填）
