# D008 英美立面逐戶造型與飾條

**基線**：`8999376`
**參考**：2D 實驗線 `lijiabao1998/GlimmerTown-lab` @ `d23c18d`（v13.43／T638），只讀。行號都指 `index.html` @ `d23c18d`。
**授權**：業主 2026-09-25「全部做完吧」；美術由 Claude 判斷（CLAUDE.md 規則 1 補充）。

## 為什麼做這張

D004 把住商工裡走「英美立面」的原型只畫成外形：一個量體加坪頂或斜頂。D005 補了核心路徑的點綴。但實驗線的立面繪製器（T577，72936–74660）逐戶畫了很多東西，現在 3D 裡都還看不出來：

- **連棟屋（ukTerrace）**：石板長脊、每戶一樓凸窗、分戶牆上的煙囪。
- **半獨立屋（ukSemi）**：兩戶鏡像一對、四坡陶瓦頂、兩層凸窗、門廊雨遮。
- **維多利亞排屋（ukVictorian）**：灰泥、女兒牆、簷口、一樓陽台帶。
- **大宅（ukMansion）**：紅磚、白石樓板帶、孟莎屋頂加天窗。
- **高街（ukHighStreet）**：一樓店面、板岩屋頂、轉角角樓；二級是旅館（孟莎屋頂、雨遮、名牌）。
- **布朗石（usBrownstone）**：門前石階、托架簷口。
- **戰前公寓（usPrewar）**：石基座、簷口、屋頂水塔、防火梯。
- **主街（usMainStreet）**：階梯式女兒牆、雨遮、招牌。

核心路徑的飾條（`classicTrim565`／`modernTrim565`，70700 起）也沒搬：
- 石或磚基座、腰線、簷口、轉角石；
- 現代風的女兒牆與門廳。

## 做什麼

1. **飾條的選法照實驗線，對拍。**
   - 抽取加記 `__t547.sty`（核心路徑的 `sty565`＝(v＋k＋寬) mod 3）。
   - 1,728 組跟配方逐項相等。
   - 飾條種類照 71460 起的分支：
     - villa、工業不畫；
     - 原型名含 modern 用現代，含 town／stone 用石；
     - 其餘 sty 1 用石；sty 2 時商業用現代、其他用磚。
2. **逐戶造型**：`src/content/facades.ts`（純函式）算每個立面街區的戶數、每戶的寬度與高低起伏，以及凸窗、煙囪、門、石階、角樓、水塔的擺放；`src/render/blockArt.ts` 依計畫畫。
   - 戶寬取實驗線的量級：連棟屋約半格一戶，半獨立屋一格一對。
   - 顏色：磚、灰泥、石板、陶瓦、布朗石用固定的幾組色（實驗線立面繪製器自帶色，不走調色盤），逐戶用雜湊輪換。
3. **飾條**：核心路徑的建築依上面的選法加基座帶、腰線、簷口、轉角石，或現代的女兒牆與門廳帶。
4. **只在 B、C 兩檔加**：
   - A 檔（一格一棟、密度對照）維持 D007 的樣子。理由：種子城 A 檔離手機預算上限只剩 156 個三角形，而 A 檔有幾百棟 1×1 的立面小屋。
   - 預設是 B。

## 不改什麼

- 切分、量體、屋頂設備、前庭、窗型都不動（對拍的黃金樣本照舊）。
- 非住商工、地面、明暗、300 年示範不動。
- A 檔不動。
- 夜景不做；立面上的招牌字、門牌不做。

## 驗收（動手前寫）

1. **對拍**：
   - 1,728 組的 `sty565` 跟配方相等；
   - 注入「sty 改用 (v＋k) mod 3」要紅。
2. **逐戶計畫**（Node 跑 1,728 組）：
   - 每個立面街區的戶數 ≥1；
   - 每戶都在主體框裡、戶與戶不重疊、加起來剛好蓋滿正面；
   - 凸窗、石階等都貼在正面、不出地界；
   - 同一組配方擺法固定。
3. **畫出來＝計畫**：瀏覽器裡，B、C 兩檔兩城，畫出的戶數、凸窗、煙囪、石階、角樓、水塔、飾條件數都跟計畫相同。
4. **A 檔不變**：A 檔兩城的三角形與 draw call 跟 D007 定稿相同（種子城 118,728、AI 城 78,324）。
5. **手機預算**：B、C 兩城 ≤ D003 基線 1.5 倍、draw call ≤ 18。
6. **點擊**：點立面上的凸窗、石階，回到那個街區的建築。
7. **對照圖**：
   - 立面多的兩個特寫（種子城住宅區、市中心），實驗線 2D｜D007｜D008；
   - 手機直式。
8. **上線**：Node 守衛、煙霧、Actions、Pages 綠。

## 施工紀錄

2026-09-25，本機施工。開工時 HEAD＝`origin/main`＝`69afff0`（這張卡的提交）。

**對拍**：抽取加記 `sty565`。1,728 組量體對拍（含 sty565）0 差異；注入「sty 改用 (v＋k) mod 3」→1,026 筆差異，紅。

**逐戶計畫**：`src/content/facades.ts`，純函式。

- **連棟屋、半獨立屋照實驗線原始碼的結構改寫**（73021、73120）。首版是自己估的，跟實驗線對讀後改了四處：
  - 深街區背靠背多排，排數＝clamp(round(深/32/.72),1,3)，每排各一道屋脊（M 形屋頂）；
  - 半獨立屋一對 24px，對與對之間 4px 車道縫；對數＝round((正面＋4)/28)；
  - 連棟屋每兩戶一座煙囪（含左端），凸窗在每戶右側；
  - 牆色、屋頂色改用實驗線立面繪製器自帶的色：連棟偶數 v 黃磚、奇數紅磚，11% 粉刷；半獨立整對同色。
- **其他六種是量級近似**：維多利亞、大宅、高街、布朗石、戰前、主街。戶數與小件照實驗線的量級估，不是逐項照原始碼搬。
- **核心飾條也寫進計畫**：`TrimPlan.rings`，照選法列出每一圈。
  - 石、磚：基座、腰線（牆 >14px 才有）、簷口、轉角石。
  - 現代：門廳帶、女兒牆壓頂。
  - 首版的圈數是畫的時候才決定的，「畫出來＝計畫」就驗不到；改成先寫進計畫。

**畫法**：`blockArt.ts` 的 `drawFacade`、`drawTrim`。

- 逐排、逐戶畫牆盒。半獨立屋下層紅磚、上層粉刷。
- 凸窗、天窗畫在有窗的牆網格裡（首版畫在點綴網格，沒有窗）；煙囪騎在各排屋脊上。
- 只在 B、C 檔：`BlockRender.detail`。

**守衛**

- **Node（1,728 組）**：
  - 立面路徑都有計畫、戶 ≥1；
  - 戶與戶不重疊，加上車道縫剛好蓋滿正面；排蓋滿深度；
  - 小件不出地界、不落在縫裡；飾條圈照選法；
  - 同一組配方固定。
  - 注入三種錯：凸窗移到戶外（紅）、半獨立屋的對寬少算一道縫（紅）、對數公式改 5px 縫（綠：它沒破壞覆蓋，這條守衛本來就不驗實驗線的數字，見「沒做成」）。
- **煙霧（瀏覽器，兩城）**：
  - A 檔三角形、draw call＝D007 定稿（種子城 118,728、AI 城 78,324），沒有立面、飾條。
  - B、C 檔畫出的戶數、排數、小件（逐種）、帶與飾條圈＝計畫。種子城 B：戶 136、排 63、小件 330、帶＋飾條 263。
  - B、C 手機預算。
  - 從正上方點正面突出的凸窗、門廊、石階，回到那個街區：種子城 B 28／28、C 40／40，AI 城 B 15／15、C 20／20。
- 煙霧 111 項（D008 新增 14 項）；Node 守衛 38 項（新增 1 項）。

**手機預算**（三角形含影子那一趟，draw call 15）

| | 種子城 | AI 城 |
|---|---|---|
| D003 基線 | 79,256 | 70,910 |
| A（不變） | 118,728（1.50 倍） | 78,324（1.10 倍） |
| **B（預設）** | **69,598（0.88 倍；D007 59,910）** | **67,396（0.95 倍；D007 64,606）** |
| C | 93,610（1.18 倍；D007 78,824） | 74,566（1.05 倍；D007 70,714） |

- 單檔 822.6 KB（gzip 232.2 KB），比 D007 多 12 KB。
- 建場景（本機）：種子城 B 77 ms，AI 城 B 78 ms。

**施工中自己抓到、照實記下的事**

1. **連棟、半獨立首版結構跟實驗線不同**：沒分排、沒有車道縫、煙囪每戶一座。對讀實驗線原始碼後改寫，見上。
2. **飾條圈數原本是畫的時候才決定的**：這樣「畫出來＝計畫」只是同一段邏輯寫兩遍。改成計畫裡列出每一圈。
3. **驗收 2 改了措辭**：原文「加起來剛好蓋滿正面」，但半獨立屋照實驗線要留車道縫。改成「戶＋車道縫剛好蓋滿正面」，每道縫同寬。
4. **驗收 7 改了取景**：
   - 原本要用種子城住宅區、市中心兩個特寫，但那兩處 6 格內只有 4 個、3 個立面街區。
   - 改用立面最密的三處：種子城 (7,14)、(8,30)，AI 城 (30,42)。
   - 為此重跑 `lab-extract --part=d004` 補拍 2D。重跑後黃金樣本（量體、兩城切分）逐位元組相同。
5. **街區裡的建築沒有逐棟的鏡頭錨點**，`focusBuilding` 對不到。新增 `focusBlock(街區, 縮放, 裁切)`。裁切是把鏡頭前面擋住的高樓切掉，只在檢查時用。
6. **D006 的幾何釘再改一次**：這張故意在 B 檔加了立面，釘改成 D008 定稿的值（種子城 69,598、AI 城 67,396）。
7. **對照圖改存 JPEG**：2×2 四格的 PNG 一張約 3 MB。

**對照圖**

- `docs/img/D008-seed-fa-compare.jpg`、`D008-seed-fb-compare.jpg`、`D008-ai-fa-compare.jpg`：實驗線 2D｜D007｜D008｜D008 放大兩倍。
- `docs/img/D008-mobile.png`：手機直式，點一個維多利亞排屋街區。

**沒做成的事**

- **只有連棟屋、半獨立屋照實驗線原始碼的結構搬。** 其他六種是量級近似。例如：
  - 高街實驗線是每 12px 一間店（我用每格 2 間）；
  - 布朗石實驗線每戶 3 開間、深街區也前後兩排；
  - 戰前公寓的成對窗沒搬。
- **立面數字是照實驗線原始碼抄的，沒有執行期對拍。** 規則 8 管的是規則；這些是美術量級，所以沒加注入。抄錯了守衛抓不到，上面第三種注入就是例子。
- **角樓、水塔、防火梯沒在瀏覽器裡畫過。** 兩座樣本城沒有一級高街（種子城 13 個高街都是二級旅館），也沒有戰前公寓。這三種只跑了 Node 計畫守衛，驗收 3 的「角樓、水塔件數＝計畫」等於沒驗到。
- **每戶沒有門。** 卡面寫了「門」，但計畫裡只有門廊、石階；實驗線逐戶的門色、扇形窗、門燈都沒搬。
- **這些也沒搬**：前庭的圍籬與垃圾桶（frontGarden）、半獨立屋的側邊車道與車、連棟屋三成的屋頂天窗。
- **正面一律朝 +z**（同實驗線的受光正面），不會轉向真正的街道。
- **A 檔沒有立面**（卡面本來就這樣定）。
- **2D／3D 取景還是有點偏**，沿用 D005 的換算。
- **沒上實機。**
