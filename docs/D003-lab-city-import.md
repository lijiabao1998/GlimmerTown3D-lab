# D003 2D 實驗線的城市出現在 3D 裡（分享碼匯入）

**基線**：`1b9cab9`
**參考**：2D 實驗線 `lijiabao1998/GlimmerTown-lab` @ `d23c18d`（v13.43／T638），只讀

## 業主決定（2026-09-25）

1. 3D 線只以 **2D 實驗線** 為參考；主線的規則、數字、Python 工具都不用。
2. 玩法 **全力做建造**；一生、考古暫停（世界歷史照樣記）。
3. 2D 的美術規矩可以參考；**原始碼不使用任何外部素材**，畫面全部由程式生成。
4. 本倉庫的定位是 **Pre**：內容做到一大段之後，業主可能從這裡 fork 出主線。

以上同步寫進 `CLAUDE.md` 與 `docs/D000-vision.md`。

## 為什麼先做這張

建造模式需要的地基，是建築種類表、地塊圖層和佔地規則。這三樣正好也是讀懂實驗線存檔需要的東西。

所以先讓實驗線的城市原樣出現在 3D 裡。業主一眼就能比對 2D 和 3D；後面的建築配方（D004）、規則（D005）、建造（D006），都長在同一個世界模型上。

## 改什麼

1. **抽取工具** `tools/lab-extract.mjs`（Node，只讀實驗線）：
   - 靜態抽出 `KNAME`、`KCB`、`KCAT`、`MSZ`，出處行號寫進輸出；
   - 無頭執行實驗線：
     - 量每張建築精靈圖的高度，像素換算成格數要除以 39.2（理由見文末）；
     - 拍種子城的 2D 樣張；
     - 產生兩個樣本碼：種子城、AI 城 120 天，並當場記下實驗線自己量到的對帳數字；
   - 輸出 `src/content/lab-kinds.json`（186 種）與 `src/content/samples/`。
2. **解碼器** `src/io/labcode.ts`（純邏輯）：處理 `GVX1:` 前綴、base64、UTF-8、JSON、RLE 與驗證；解不開時回傳明確原因。
3. **城市模型** `src/sim/city.ts`（純邏輯）：
   - 地塊圖層用 typed array；
   - 建築記根格、佔地、種類、等級、變體、年齡、是否廢棄；
   - 匯入記成世界歷史的第一筆事件。
4. **渲染** `src/render/cityScene.ts`，畫風 A：
   - 地面：地形（含高地崖面）、水、道路（四種，加上等級）、鐵路、分區底色、樹；
   - 建築量體：高度取自精靈圖量測，加上分類色環與依分類決定的屋頂。
5. **觀看頁**：
   - 預設進「2D 城市」模式，顯示種子城；
   - 可切換到 AI 城，也可以貼上自己的分享碼；
   - 300 年示範改由按鈕或 `?mode=history` 進入；
   - 點建築可看名稱、分類、等級、佔地，以及約建於第幾天。

## 不改什麼

- 世界生成（D001）與歷史格式 v1 不動；300 年示範的所有守衛照舊。
- 不做建造操作，也不做模擬規則（留給 D005、D006）。
- 不畫逐種建築配方（留給 D004）；這張的建築是量體佔位。
- 不碰任何 2D 倉庫（只讀）。

## 驗收（動手前寫）

1. **解碼對帳**：Node 和瀏覽器各跑一次。兩個樣本碼解出的數字，要跟實驗線執行期當場量到的逐項相等，差異 0：
   - 各種類的根格數；
   - 每棟建築的佔地格數；
   - 水、四種道路、鐵路、樹、各分區、高地的格數。
2. **RLE**：自寫一個與實驗線 `rleEnc` 等價的編碼器，跑 1,000 條隨機字串往返，0 錯。
3. **壞碼不崩，而且講得出原因**：空字串、非 base64、截斷、壞 JSON、`v≠1`、圖層長度不符、超過 2,000,000 字元、座標出界，都要回傳明確原因。未知的建築種類照樣匯入，並計數。
4. **非 72 的地圖**（例如 108）也能匯入。實驗線自己的匯入要求地圖跟目前開著的同尺寸，這裡不要求。
5. **內容表**：
   - 186 種全部有名稱、分類、佔地；
   - 分類與實驗線執行期 `GV.kcat345` 逐種一致；
   - 高度來自精靈圖量測，沒有圖的種類要標記出來，改用分類預設值。
6. **3D 畫面**：
   - 種子城、AI 城都非空白；
   - 場景裡的建築數等於城市的建築數；
   - 用程式把一棟已知建築投影到螢幕再模擬點擊，履歷卡上的名稱與格子要正確。種子城、AI 城各驗一次。
7. **世界歷史**：
   - 匯入記成第一筆事件，內容包括來源、實驗線版本、種子、天數、碼的雜湊；
   - 地塊卡顯示「約第 N 天蓋起（由 age 推算）」。
8. **零外部素材**：`src/` 內沒有圖片、字型、模型檔；煙霧測試記錄所有網路請求，只准連 127.0.0.1。
9. **效能**：
   - 記下種子城「解碼＋建城市＋建場景」的耗時與繪製數字（呼叫次數、三角形數）；
   - 解碼＋建場景要在 1,500 ms 內（CI 用軟體渲染）。
10. **舊守衛**：D001、D002 的全部守衛照舊綠，改用 `?mode=history` 進入。
11. **樣張**：
    - 種子城 2D、3D 並排，同一視角；
    - AI 城 3D；
    - 手機直式一張，加上履歷卡。

## 高度換算的理由

實驗線的投影是 `P(u,v,h)=[AX+(u−v)·32, TOPY+(u+v)·16−h]`（實驗線 `index.html:50238`），屬於 2:1 斜俯視，等於正交鏡頭方位 45°、仰角 30°。本線的鏡頭（`src/main.ts`）也是這個角度。

- 地面一格的邊長投影成 (32, 16) 像素，所以一個世界單位等於 32 ÷ cos 45° ≈ 45.25 px。
- 垂直方向要再乘 cos 30°，所以一格高 ≈ 39.2 px。
- 因此精靈圖的像素高度除以 39.2，就是 3D 的格數。

座標對應：實驗線的格子 (x, y) 對到本線的 (x, z)。兩邊的預設視角一致：+x 往右下、+z 往左下，光從左邊來。
