# D006 全城色調、草地、道路

**基線**：`f9e5add`
**參考**：2D 實驗線 `lijiabao1998/GlimmerTown-lab` @ `d23c18d`（v13.43／T638），只讀。
**授權**：業主 2026-09-25「全部做完吧」，D005 沒做成的事接著做；美術由 Claude 判斷（CLAUDE.md 規則 1 補充）。

## 為什麼做這張

D005 做完後跟 2D 並排，差最多的是整片地面與整體明暗：

1. **草色偏暗偏黃。**
   - 3D 的草是 `#78a452`～`#6c9749` 的雜色；
   - 實驗線的草是 `#74b258` 一族（D004 抽取拍的 2D 圖逐色統計：遠景 24.8% 的像素就是 `#74b258`），每一格還有一條淡淡的亮邊（`#82c467`、`#82b357`），讀起來像一塊塊草皮。
2. **道路是淺褐灰。**
   - 3D 的路面是 `#6b6a66`，邊 `#8a8780`；
   - 實驗線的柏油是深藍灰（`#4e525b` 一族），有人行道、車道線。
3. **水、沙也都差一點。**
   - 實驗線近景的水是 `#3f7ec0`／`#407fc1`，沙是 `#d9c689`。
4. **立面明暗對比弱。**
   - 實驗線是「左亮右暗」：受光面用 `pal.light`、背光面用 `pal.dark`（T541），兩面差很多，例如住宅 `#e7c6a2` 對 `#77443c`。
   - 3D 背光面只比受光面暗兩成左右，整座城讀起來灰灰平平。

## 做什麼

1. **地面貼圖解析度**：每格像素從 4 提到 8（地圖 ≤128 格時；貼圖邊長上限 1,024），才畫得出草皮格線、人行道、車道線。
2. **地面色**（顏色取自上面的逐色統計）：
   - 草：`#74b258` 一族，每格左、上兩邊一條亮邊 `#82c467`；分區底色照舊淡淡疊上。
   - 水：`#3f7ec0` 一族，亮點照舊。
   - 沙：`#d9c689` 一族。
   - D005 的地坪（住宅、商業、工業、villa）與 D0 草坪照舊，草坪改用新草色。
3. **道路**：
   - 柏油 `#4e525b` 一族；
   - 靠非道路的一側畫 1 像素人行道 `#c6c0ac`；
   - 直路中線：一般路白色虛線，幹道（`rclass ≥ 4`）黃色實線；
   - 橋：柏油加淺色護欄；高速公路照舊黃邊。
4. **立面明暗**（只改 2D 城市模式，300 年示範不動）：做三檔，拍同一個畫面對照後挑一檔。
   - 甲：現況。
   - 乙：背光面壓暗。三階色塊的中間階從 175 降到 120，天光從 1.1 降到 0.9。
   - 丙：乙，再把受光面提亮。太陽 2.2 升到 2.6。
5. **對照圖**：實驗線 2D、D005、甲乙丙，同一個畫面；另拍中景、遠景、特寫、手機直式。

## 不改什麼

- 建築的幾何、配方、點綴、切分、對拍都不動（三角形數、draw call 逐位不變）。
- 300 年示範不動（它有自己的地面貼圖與材質）。
- 樹的形狀、地形高低不動。

## 驗收（動手前寫）

1. **地面色逐格驗**（瀏覽器讀地面貼圖）：
   - 草格的像素都在草色族或格線色裡；
   - 水、沙、柏油也都在各自的色族裡。
2. **道路**：
   - 每一格路，面向非道路的那一側邊上全是人行道色；
   - 直路（只有對向兩鄰是路）的中線上有車道線；
   - 路口不畫車道線。
3. **幾何不變**：三檔兩城、`?blocks=off` 的三角形與 draw call 跟 D005 相同；300 年示範的雜湊、三角形、draw call 不變。
4. **明暗**：挑定的那一檔寫進卡面，附對照圖與理由。
5. **手機**：地面貼圖邊長 ≤1,024。
6. **上線**：Node 守衛、煙霧、Actions、Pages 綠。

## 施工紀錄

2026-09-25，本機施工。開工時 HEAD＝`origin/main`＝`3255c0d`（這張卡的提交）。

**顏色從哪來**

- D004 抽取拍的實驗線 2D 圖做了逐色統計：種子城遠景、工業區特寫、住宅區特寫。
- 取用的色：
  - 草 `#74b258`（遠景 24.8%）與近景的 `#73a959`、`#78aa59`、`#74ae59`、`#75ab5a`；高地草 `#8aba58`；
  - 水 `#3f7ec0`、`#407fc1`、`#3e7cbf`、`#3d7bbe`；沙 `#d9c689`；
  - 柏油 `#4e525b`、`#4d515a`、`#50545c`、`#4c5059`。
- 這些色都集中在 `src/render/ground.ts`（純函式），守衛直接讀同一份色族。
- **草皮格線**：實驗線近景的格線有 `#82c467`、`#7cae59` 幾種。照 `#82c467` 畫，中景看整片像方格紙，改用介於草色與格線之間的 `#7cb95e`。

**明暗：挑 d**

- 拍了甲乙丙三檔，外加施工中加的第四檔 d。
- 丙（太陽 2.6）受光面太亮，米白牆被削成粉紅（色版溢位）。
- 乙把天光降到 0.9，連屋頂和草地一起變暗。
- d：三階中間階 175→125、暗階 90→80，天光和太陽維持原樣。
  - 只有背光的右側面變暗，屋頂、草地、左側受光面不變。
  - 最接近實驗線「左亮右暗」、頂面明亮的讀法。
- 預設是 d；`?tone=a|b|c|d` 可切換。只影響 2D 城市模式，300 年示範用 D001 的原設定。

**守衛**（兩座城，在 Node 逐像素驗整張地面貼圖）

- 貼圖 576×576（每格 8 像素）。
- **色族**：草、水、沙每格都在實驗線色族裡，0 格不對。
  - 種子城：草 2,300、水 923、沙 304 格；
  - AI 城：草 2,548、水 1,026、沙 501 格。
- **道路**：
  - 面向非道路的邊全是人行道、護欄或黃邊；路面全是柏油或車道線色；0 像素不對。
  - 種子城：直路 369 格全有車道線，路口 38 個都沒畫。
  - AI 城：直路 349 格全有車道線，路口 42 個都沒畫。
- **幾何不變**：預設 B 的三角形與 draw call 同 D005（57,176／58,772，15 次）；`?blocks=off` 仍是 D003 基線；300 年示範仍是 48,794 個三角形、11 次。
- 煙霧測試 84 項（50 秒），D006 新增 7 項；Node 守衛 34 項照舊綠。

**量測**

- 單檔 731.2 KB（gzip 202.7 KB），比 D005 多 1.6 KB。
- **建場景變慢**：地面貼圖從 288×288 變成 576×576，像素是 4 倍。種子城預設 B 從 60 ms 變 78 ms，A 檔 107 ms；AI 城 B 58 ms。手機上會更慢，沒量。

**對照圖**（實驗線 2D、D005、明暗 a／b／c／d，同一個畫面）

- `docs/img/D006-mid-compare.png`：種子城中景。
- `docs/img/D006-res-compare.png`：種子城住宅區特寫。
- `docs/img/D006-ai-compare.png`：AI 城住宅區特寫。
- `docs/img/D006-mobile.png`：手機直式。

**沒做成的事**

- **地面的變化還少。** 實驗線草地上有小花、石頭、草叢，水面有波紋與倒影，沙灘有浪花；這些都沒做。
- **道路只有直路中線。** 斑馬線、路口停止線、彎道都沒畫；鐵路、電車軌、碼頭是 D003 的畫法。
- **非住商工用地仍是 D003 的淺灰鋪面**，留給 D007。
- **沒上實機量**，建場景時間的增加在手機上沒驗。
